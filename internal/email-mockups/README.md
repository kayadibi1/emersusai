These mockups are source-only internal assets.

They are intentionally stored outside any deployed static route so they do not
end up guessable or directly accessible on production.

Files:
- `template-a.html`: waitlist confirm identity mockup
- `template-b.html`: waitlist confirmed mockup
