# Changelog

Append-only log of meaningful completed changes. Format: `YYYY-MM-DD — summary — key files`. Skip trivial edits.

- 2026-04-26 — **Hetzner Cloud → Robot dedicated migration LIVE.** Cutover from CPX41 (NBG1, 8 vCPU shared / 16 GB / ~240 GB SSD / €30/mo) to AMD Ryzen 9 3900 dedicated (HEL1-DC7, 12C/24T / 128 GB DDR4 ECC / 2× 1.92 TB U.2 NVMe RAID 1 / €86 ex-VAT/mo). **~14 minutes user-facing downtime via Postgres physical streaming replication over Hetzner vSwitch — HNSW index (33 GB) preserved byte-identical, zero rebuild cost.** Post-cutover IP `37.27.112.76`, hostname `emersus-prod`. Cloud box stays in 7-day soak (supabase-db only) until 2026-05-03 then decommission. **Strategy**: pg_basebackup over private vSwitch + WAL stream → standby caught up → `pm2 stop` apps on cloud → final `pg_switch_wal` → `pg_promote()` on standby (point of no return) → `docker compose up` full stack on new → DNS A records flipped at Porkbun (TTL 600 → propagated in <2min) → Caddy ACME tls-alpn-01 + http-01 challenges succeeded ~30s after DNS resolved → all green. **Cutover timeline (UTC)**: 04:48:47 stop pm2 → 04:51:06 promote → 04:51:32 docker compose up → 05:01:34 ACME certs obtained → 05:03:00 first 200 on `https://emersus.ai/`. **Stack components migrated**: full Supabase docker compose (auth/kong/realtime/storage/edge-functions/imgproxy/meta/studio/analytics/vector/pooler/rest/db) + emersus-api/emersus-worker/webhook pm2 processes + Caddy with Let's Encrypt + systemd backup timer + ~/.git-credentials/ecosystem.config.js/scripts/backup-db.sh + chunks-phase2f.jsonl (7.5 GB) + /etc/postgresql-custom volume (pgsodium_root.key + supautils.conf + extension scripts). **Replication setup**: `replicator` role with REPLICATION attribute + physical replication slot `emersus_new_box_repl` + pg_hba entries for vSwitch IP 10.0.1.10 AND docker bridge 172.16.0.0/12 (socat sidecar `pg-vswitch-proxy` on cloud bridged 10.0.0.2:5434 → supabase-db:5432, NAT'd source IPs to bridge addresses). **Lessons codified in memory**: PowerShell binary pipes corrupt non-ASCII bytes (use scp -3); Termius bracketed paste splits long commands at `>`/`>>` redirects; Supabase docker bind mounts auto-create dirs when source is missing; postgresql.conf is image-baked at /etc/postgresql/, custom overrides go in /etc/postgresql-custom/ named volume; hot_standby=off in supabase image (set via postgresql.auto.conf override); npm run build required after fresh git clone (dist/ gitignored); chmod 755 /home/emersus/ for Caddy file_server traversal; Hetzner Cloud subnet "vSwitch type" checkbox must be enabled with VLAN ID 4000; vSwitch MTU 1400 (not 1500); Hetzner VAT auto-detected for US private (no ticket needed). **Verified post-cutover**: row counts byte-identical (1,445,053 research_articles + 3,037,812 evidence_chunks), HNSW indexes intact, public smoke green (emersus.ai/ + /api/health + supabase.emersus.ai/auth/v1/health all 200/expected), TLS valid Let's Encrypt E8, daily backup timer enabled, pm2 startup script via systemd. **Scheduled remote agent** (trig_01Wwf6N2Y4J7vqxR4o2jagpH) fires Sun 2026-05-03 14:00 UTC for health-check + decommission walkthrough. **SSH aliases on desktop**: `hetzner` → 37.27.112.76, `hetzner-cloud-old` → 46.225.58.187 (decommission target). **Deferred**: Hetzner Storage Box for offsite backup (~€3.81/mo); phase2f sweep resume; Gemini paced+auto-sync resume on desktop with `--after-id=492507`. **Memory notes**: `project_hetzner_robot_migration_complete.md` (full migration record), `reference_hetzner_server.md` (updated with new IP/specs), MEMORY.md index. — infra-only ship (no app code changes); `~/.ssh/config` (desktop), Hetzner Cloud Network "emersus-migration" (10.0.0.0/16 + vSwitch subnet 10.0.1.0/24), Hetzner Robot vSwitch VLAN 4000

- 2026-04-24 (later) — **Retrieval quality overhaul: HyDE live + Jina rerank integrated (dormant) + matrix eval harness + 200-fixture stratified benchmark.** Ship arc of 3 commits (`a025e06d` → `8e55498f` → `9247b37b`) addressing the April 23 vocabulary-gap diagnostic ("sugar and athletic performance" missing 8 known-on-topic specific-vocab RCTs from top-50). **Starting point**: baseline dense retrieval surfaces the user's target paper in top-10 only 30% of the time on realistic queries — a finding that only emerged after building a proper fixture set (old 20-fixture set was cherry-picked easy, showed fake 85% baseline). **Shipped 3 retrieval layers, 1 active, 2 flag-gated**: (1) **HyDE** (`api/emersus/pipeline/hyde.js` + `retrieveDatabaseEvidence.js` rewrite) — `CHAT_HYDE_ENABLED=true` flag LIVE in prod. gpt-4.1-mini generates 60-120 word hypothetical biomedical passage, raw prompt + hypothetical both embedded via text-embedding-3-small, each hits v4 RPC at 2× matchCount, RRF-fused k=60. Graceful single-query fallback on LLM failure. Measured lift: recall@10 30.0% → 35.5%, recall@100 36.0% → 50.0%, MRR@10 0.198 → 0.196 (pool coverage up 14pp, top-K concentration same). Cost: +$0.0004/query (~$40/mo at 100K chats). Latency: +2.2s. Prod smoke: "does sugar help endurance athletes" now surfaces `CARBOHYDRATE MOUTH RINSE` rank #2 sim 0.844 + `Fructose co-ingestion` rank #4 sim 0.818 — the exact specific-vocab papers absent from v4 baseline per April 23 diagnostic. (2) **Jina cross-encoder rerank** (`api/emersus/pipeline/jina-rerank.js`) — `CHAT_JINA_RERANK_ENABLED` flag SHIPPED BUT OFF. Option C architecture: Jina runs between retrieval and the existing `rankEvidence` heuristic blend, so freshness/quality/RCR UI credibility signals stay intact while Jina feeds the heuristic a relevance-ordered pool. Graceful-fail on 429/403/5xx — catches internally, returns unranked candidates, chat never blocks. Flag stays off because Jina's 10M free-tier tokens got burned during the matrix eval (the API also has a 100K-tok/min rate cap; needed 65s-sleep retries during the matrix to cope). **Measured lift on top of HyDE**: recall@10 35.5% → 37.5% (+2pp), MRR@10 0.196 → 0.235 (big shift — rank 2 → rank 1 type promotions). Activates with one flag flip once user tops up Jina balance. (3) **VECTOR_MATCH_COUNT bumped 10 → 25** in `pipeline/retrieve.js` — pool of 10 left the cross-encoder with nothing to reorder. Independent baseline improvement even with Jina off (heuristic picks top-6 from 2.5× larger pool). (4) **Stratified random fixture generator** (`scripts/eval/generate-fixtures.js`) — samples research_articles uniformly across year buckets (pre-2000 / 2000s / 2010s / 2020s at 8/14/33/45% target weights), scope-filters to exercise/nutrition via SCOPE_MESH (Exercise / Athletic Performance / Muscle Strength / Resistance Training / Dietary Supplements / Body Composition / +25 more MeSH) + SCOPE_TITLE_PATTERNS keyword fallback for OpenAlex rows without MeSH, LLM-generates one query per paper via gpt-4.1-mini with randomly-assigned difficulty (easy/medium/hard/applied/skeptical at 18/14/28/29/10%) × format (question/statement/keyword 54/24/22%) × population angle (general/subgroup/specific 62/20/17%) × length (short/medium/long 29/54/16%), validates query-to-paper match via dense retrieval top-5 (canonical_dedup_key siblings become must_include; semantic neighbors become must_exclude). Output: 200 fixtures in `scripts/eval/fixtures/retrieval-v2.json`, generated in 10.7 min for $0.11 LLM cost, zero rejections. Query quality note: first run leaked style tags into outputs ("skepticism" literally in a query) — fixed with explicit forbidden-list in system prompt. Scope filtering went through an A/B: random across full 1.23M-paper corpus pulled ~50% biomedical non-fitness content (stroke rehab, ob/gyn, etc.); user confirmed scope filter to match production `safety.js` exercise-science lock. (5) **Stack-config matrix harness** (`scripts/eval/bench-matrix.js`) — runs N stacks × M fixtures, measures recall@{6, 10, 50, 100} + MRR@10 + latency + cost per cell, emits CSV+MD+JSON. Stack configs in `stacks.json` cover 17 permutations across {dense / hybrid} × {none / multi-query / HyDE / PICOs / MQ+PICOs} × {no-rerank / Cohere / Jina / Voyage}. `--only=S0,S3` filter, `--limit=N` for quick smoke, `--apply-prod-rerank` flag runs the production `rankEvidence` heuristic after retrieval+rerank (e2e measurement). Matrix auto-skips stacks whose reranker backend isn't configured. Fix: aggregation crashed on all-errored stacks (undefined field on `.toFixed()`); guarded with `safe()` helper. (6) **Paired-diff analyzer** (`scripts/eval/analyze-paired-diff.js`) — consumes 1-3 matrix JSON outputs (free + jina + e2e), joins on (stack_id, fixture_question), produces paired deltas and auto-recommends integration option (A: Jina after heuristic / C: Jina before heuristic / E: swap similarity weight). Without e2e data available (Jina balance exhausted before the final e2e matrix completed — bug crashed aggregation, couldn't re-run), chose Option C on principle. (7) **Matrix findings drove the ship decision.** 200 fixtures × 6 stacks × 2 runs (retrieval-layer + failed e2e) generated 2,400 cells of empirical data. Key learnings: Jina rescues query-transform stacks hardest (S2 multi-query +8pp, S4 PICOs +5pp) — their recall@100 was strong but top-K was RRF-diluted. HyDE already concentrates relevance well, so Jina adds "only" +2pp on top of HyDE — but MRR climbs 0.196 → 0.235 (rank promotions). S14 HyDE+Jina is the Pareto winner at 37.5% recall@10. **39.5% of realistic fixtures have the target absent from top-100 across ALL tested stacks** — the "unsalvageable" slice that needs index-time fixes (contextualization / MedCPT domain embeddings / hybrid BM25). Rerank cannot rescue what's not in the pool. (8) **Hybrid v5 SQL** (`supabase/20260424_match_evidence_chunks_hybrid_v5.sql`) — committed but not applied. BM25+dense RRF via expression GIN index on `to_tsvector('english', content)`, `CREATE INDEX CONCURRENTLY` so safe during live traffic, build ~20-90 min. Addresses pool composition for the unsalvageable slice. Parked pending user decision. (9) **Research agent finding** (via background general-purpose agent, wrote `docs/preprocessing-solutions-research.md`): surveyed 8 preprocessing techniques with primary-source empirical numbers. MedCPT (Jin 2023, Bioinformatics, Apache 2.0) is SOTA on 3/5 BEIR biomedical tasks (TREC-COVID 0.709, NFCorpus 0.355, SciFact 0.761) — stronger biomedical-specific evidence than Anthropic contextualization which Anthropic tested only on internal arXiv/Science benchmark. User's "batch gpt 4.0" Phase 3 plan cost-checked: GPT-4o batch = ~$3,350 for 3.34M chunks; **Gemini Flash 2.0 batch already wired up in repo at $65** (30× cheaper), GPT-4o-mini batch at $300 is 10× cheaper than GPT-4o. Parallel discovery: `contextualize-evidence-gemini-batch.js` + `20260423_contextual_embeddings.sql` were already shipped by a prior session (schema for `evidence_chunks.context_prefix` + `contextual_embedding vector(1536)` ready to populate). Phase 3 blocked on provider choice — flagged in memory that existing pipeline is Gemini-tuned, swap to OpenAI batch is a new adapter. **Concurrent deploy**: the final Jina ship push co-deployed with a parallel Claude session's email-templates subsystem (~3200 LOC across `api/lib/email/**` + 12 templates + webhook handlers + tests); deploy blocked on package-lock.json drift (per memory `reference_hetzner_stash_drift.md`), manually unblocked via stash+pull+build+restart. Both ships landed in deploy event `head=9247b37b`, emersus-api restart #382, uptime clean. **Memory notes**: `project_hyde_retrieval_live.md`, `project_jina_rerank_integrated.md`, `reference_retrieval_matrix_harness.md`. — `api/emersus/pipeline/{hyde,jina-rerank}.js` (new), `api/emersus/retrieveDatabaseEvidence.js` (rewritten), `api/emersus/pipeline/retrieve.js` (matchCount bump), `scripts/eval/{bench-matrix,generate-fixtures,analyze-paired-diff,smoke-hyde,smoke-jina,smoke-hyde-jina}.js` (new), `scripts/eval/lib/{query-expand,cross-rerank}.js` (new), `scripts/eval/stacks.json` (new, 17 stack configs), `scripts/eval/fixtures/retrieval-v2.json` + `retrieval-v2-meta.json` (new, 200 fixtures + generation metadata), `scripts/eval/fixtures/retrieval.json` (upgraded sugar fixture), `supabase/20260424_match_evidence_chunks_hybrid_v5.sql` (new, unapplied)

- 2026-04-24 — **Comprehensive mobile + cross-browser + a11y pass — 17 commits, all auto-deployed.** Ship arc: `f81c6001` → `ebeddc55`. **Decomposed into 4 sub-projects + cross-cutting polish + a11y deep-pass:** (1) **Landing + static**: iOS Safari auto-zoom killed (every `<input>` ≥ 16 px — biggest win is `.chat-composer-shell textarea` at `chat.css:2102`, the primary input on the product); WCAG 2.5.5 44-px touch floors on `.btn` / nav links / `.btn-accent` CTA / `.source-read` / `.bento-card .chip` scoped to `@media (pointer: coarse)` so desktop density is untouched; `100vh` → `100dvh` fallback pair across 8 stylesheets (`auth/site/static/nutrition/profile/progress/train.css`) to fix iOS URL-bar collapse; `theme-switcher-widget.js` self-contains its CSS (was rendering as 16×6 unstyled buttons on static pages where `chrome.css` isn't loaded); `.rotator` respects 320 px viewport; global `-webkit-text-size-adjust: 100%` + `-webkit-tap-highlight-color: transparent` via `design-tokens.css`. (2) **Auth funnel** (`/auth/login/signup/forgot-password/callback/reset-password`): `.oauth-btn` + `.auth-primary` + `.auth-panel-foot > a` 44-px floor; signup full-name gains `autocapitalize="words"` + `spellcheck="false"`. (3) **Core app static audit** (`shared/chat.css`, `shared/chrome.css`): chat composer textarea 15 → 16 px; `.side-search input` 13.5 → 16 px on coarse pointer; 44-px floors on `.top-bar .icon-btn` / `.share-btn` / `.slash-item`; full `.btn` suite (`.btn`, `.btn-sm`, `.btn-icon`, `.btn-accent`, `.btn-danger`) + `.period-group button` in chrome.css; missing `-webkit-backdrop-filter` pair added at `chat.css:1911`. (4) **Domain pages**: `.tr-set-weight-input` / `.tr-set-reps-input` 16 px + 44 px on touch (every workout set entry); `.nu-supp-input` / `.nu-food-amount-input` / `.nu-add-food-input` 16 px; `.nu-search-result` 44 px; `.pf-number-field-input input` + `.pf-memory-input` 16 px; `.pg-modality-tab` + `.pg-period-pill` 44 px. (5) **Cross-cutting**: `viewport-fit=cover` added to 25 HTML pages (activated existing `env(safe-area-inset-*)` rules that were no-ops); `maximum-scale=1` stripped from 3 workout pages (WCAG 1.4.4 pinch-zoom anti-pattern); global `touch-action: manipulation` on `button / a / [role=button] / summary / input[type=...]`; `.msg-action` touch floor; footer nav tap targets; `info-card / body-copy` email link tap floor; `.bento-card .chip[aria-pressed]` → `[aria-checked]` (role="radio" uses aria-checked, not aria-pressed — was failing axe critical). (6) **`/demo` orphan removed** (`fc40cd2e`) — no surface linked to it; also pruned from `vite.config.js` + `scripts/inject-no-flash-boot.js`. (7) **a11y deep-pass via axe-core integration** (`992ebf45` on): installed axe-core 4.10 as devDep, inject library source via `page.evaluate` (bypasses site CSP), runs automatically on every `--profile=landing|auth` audit. Baseline: 57 serious/critical WCAG violations across landing + auth. Fixed iteratively in 5 passes: `aria-allowed-attr` + `aria-required-attr` on chip-constraint radios; `.compare-cell.head-cell` / `.mark-no` / `.oauth-divider span` / `.footer-disclaimer` / `.mini-card-label` / `.cta-note` / `.field-label` / `.feat-label` contrast failures → `--muted`; `link-in-text-block` underline broadened to `a[href^="mailto:"]` + `.page-main a` + `.body-copy a` + `.info-card a` + `.auth-panel-foot a` (nav/footer-col explicitly opt out). **Root-cause fix**: `--dim` design token itself was 3.8:1 mint / 2.8:1 paper — failed WCAG AA. Bumped to `#7c7c82` / `#706658` (4.9:1 / 4.6:1). **189 call sites inherited the fix automatically** vs chasing each individually. **Infrastructure shipped**: (a) `scripts/perf/landing-audit.mjs` — cross-browser × cross-viewport audit with `--profile=landing|auth|app|domain`, walks ancestor `overflow-x` to filter decoratively-clipped elements, exempts inline-body `<a>` per WCAG 2.5.5, injects axe-core for WCAG 2A/2AA/2.1A/2.1AA scan per desktop run; (b) `scripts/perf/landing-perf.mjs` — headed-Chromium FPS at 1× / 4× / 6× CPU throttle via CDP; (c) `/api/config` in-process warmer in `api/config.js` — `startConfigWarmer()` pre-fills corpus-stats cache + keeps pg.Pool hot every 2 min, eliminates the 3.15 s cold-start tax after 30 s idle (verified in prod: first hit 420 ms, steady 350 ms); (d) removed redundant `@import` of Google Fonts in `design-tokens.css` + `site.css`, added Space Grotesk to `auth/login`, `auth/signup`, `app/index.html` `<link>` tags; (e) scoped `will-change: background, transform` on 264 `.ark-how .read-grid .cell` elements → `will-change: transform` only on `.warm`/`.live` states. **Final metrics** (3 browsers × mixed viewports across 182 runs): **0 axe serious/critical violations** on landing + static + auth (was 57). **0 overflow** (was 235, via audit ancestor-walk filter + actual fixes). **0 JS errors, 0 horizontal scroll, 0 page errors**. smallTapTargets 479 → 165 (residuals are Firefox-emulation artifacts — Playwright's FF driver lacks `isMobile` so `pointer:coarse` doesn't fire — and 41×44 iPad-portrait nav labels 3 px narrower than WCAG-strict width). **Deferred** (memory notes saved): Caddy CSP missing `www.google.com` in `connect-src` blocks GA4 beacons on auth pages (38 console errors per 70 runs; one-line `/etc/caddy/Caddyfile` edit); authenticated runtime audit of `/app/*` + domain pages (would need test-account session JWT or service-role generate). **Memory notes**: `project_mobile_browser_audit_complete.md`, `project_caddy_csp_ga_blocked.md`, `reference_landing_perf_bench.md`, `feedback_headless_playwright_fps_unreliable.md`. — 18 files changed across `shared/{auth,chat,chrome,design-tokens,landing,nutrition,profile,progress,site,static,train,theme-switcher-widget,widget-v2/tokens}.css`, `shared/theme-switcher-widget.js`, `api/config.js`, `server.js`, `scripts/perf/{landing-audit,landing-perf}.mjs` (both new), `scripts/inject-no-flash-boot.js`, `vite.config.js`, `index.html` + 25 HTML pages, `auth/auth.js`, `pricing/index.html`, `package.json` + `package-lock.json` (axe-core devDep); `demo/index.html` deleted.

- 2026-04-23 (very very late) — **Evidence-grounding enforcement LIVE in prod + five assessment mechanisms + split-prompt optimization.** Ship arc of 7 commits (`c31476a1` through `2d41f348`) targeting a specific user complaint: "majority of answers have been leaning on pretrained data." **Diagnosis**: the prior `EVIDENCE PRIORITY POLICY` prose in `prompt.js` was prose-only (no enforcement), and the existing `verifyAnswerGrounding` helper computed a metric but never acted on it. Cross-referenced `docs/openai-api-reference.md` §6161 (Citation Formatting) and §19839 (Lock research and citations to retrieved evidence): canonical pattern is inline citation markers keyed to stable source IDs + post-hoc validation. **Shipped**: (1) **Grounding contract** at top of system prompt — every empirical claim requires a `citesrcN` marker (OpenAI's Unicode PUA format U+E200/E202/E201, §6161) keyed to `<source_untrusted id="N">` blocks; unsupported claims must be omitted, labeled as "coaching inference," or the model must say "the retrieved evidence does not establish X." Gated behind `GROUNDING_ENFORCEMENT_ENABLED=true`. (2) **Citation-mode verifier** (`api/emersus/pipeline/grounding-verifier.js`) parses markers, computes per-answer status (grounded/partial/ungrounded/no_claims), `cited_fraction`, invalid-marker count, strict vs legacy format counts. (3) **Client rendering** (`shared/react-chat-app.js`) — `[N]` superscripts in prose with click-to-scroll to matching source footer row + brief highlight flash; `GroundingBadge` component above `WhyThisAnswer` showing grounded/partial/weak-grounding pill. Accepts both strict PUA markers and legacy `[N]` for back-compat with pre-switch threads. (4) **100-prompt eval harness** (`scripts/eval/grounding-eval.js` + 104-prompt fixture at `scripts/eval/fixtures/grounding-prompts.json`) with three graders: LLM-as-judge (unsupported-claim count), per-claim fidelity (supported/weak/decoy/contradicted verdicts), embeddings paraphrase-similarity. **Baseline 100-prompt result** (`grounding-eval-full-100-v2-*.json`): judge 90.6% reduction vs bare gpt-5.4-mini (1,323 → 125 unsupported), fidelity 68.9% supported / 31.1% weak / **0% decoy** / **0% contradicted**, 26 grounded / 40 partial / 33 ungrounded / 1 no_claims, mean_invalid_markers 0.00, paraphrase mean_sim 0.667 / low-sim rate 1.4%. (5) **Adversarial eval** (`scripts/eval/adversarial-eval.js` + 20 fixtures at `adversarial-prompts.json` with deliberately-wrong findings like "creatine null-effect RCT"): bare gpt **0% faithful / 75% overrides**, Emersus **85% faithful / 0% overrides** — model reports the adversarial finding instead of silently correcting to textbook. (6) **Source-swap canary** (`scripts/eval/source-swap-canary.js`): rotates source order by 3 positions, re-runs, measures whether citations follow CONTENT or POSITION. 15 prompts: mean_follow_rate **0.851** (citations track content; 0 zero-follow cases). (7) **Prod shadow sampling** (`supabase/20260423_chat_grounding_samples.sql` + `maybeSampleGroundingTurn` hook in `workflow.js` + `scripts/grade-grounding-samples.js` + `scripts/grounding-trend.js`). `GROUNDING_SAMPLE_RATE` env var, default 0; set 0.01-0.05 to enable. Worker grades ungraded rows asynchronously via fidelity + paraphrase. Trend script prints 7d/14d/30d aggregate + WoW support-rate delta + alert if down ≥10pp. (8) **Split-prompt optimization** (`GROUNDING_SPLIT_PROMPT=true`): ships contract as its own system message rather than embedded at top of SYSTEM_IDENTITY. 30-prompt trial vs non-split baseline on same prompts: judge 88.9% → 92.7% (+3.8pp), grounded 6/30 → 11/30 (+5), cited_fraction 0.51 → 0.57, mean inference_labels 1.03 → 1.30. Net token cost: +4 input / −16 output per turn (effectively cheaper). (9) **Resume + parallelized graders** in eval harness — `--resume=auto` continues from newest matching snapshot; judge/fidelity/paraphrase fold into one `Promise.all` (halves per-prompt wall-clock). (10) **Enriched PostHog analytics** on `chat_stream_complete`: `cached_tokens`, `grounding_status`, `grounding_cited_fraction`, `grounding_split_enabled`. **Prod state**: both flags ON in `~/app/.env`, `emersus-api` restarted with `--update-env`; runtime verified `split: true` and 4 system messages (contract / identity / widgets / user). v3 OpenAI prompt caching already active via `synthesize.js` `prompt_cache_key: emersus-coach-v1` with 24h retention. 275/275 unit tests pass. — `api/emersus/pipeline/{prompt,grounding-verifier,stream,retrieve,retrieval-policy}.js`, `api/emersus/workflow.js`, `shared/{react-chat-app.js,chat.css}`, `scripts/eval/{grounding-eval,adversarial-eval,source-swap-canary,fixtures/grounding-prompts,fixtures/adversarial-prompts}.{js,json}` (new), `scripts/{grade-grounding-samples,grounding-trend}.js` (new), `supabase/20260423_chat_grounding_samples.sql` (new), `tests/unit/api/emersus/pipeline/{grounding-verifier,prompt}.test.js`

- 2026-04-23 (very late) — **Hard-deleted all 951,843 soft-deleted aggregator papers + their chunks.** Stage 3 cleanup: after the borderline LogReg cull brought aggregator soft-deletes to 951,843 (openalex 809,673 + openaire 110,985 + core 31,185 — all from today's three culls combined: junk-types 228k + non-English 176k + centroid >0.60 169k + borderline LogReg 197k + earlier cleanups), purged them permanently from `research_articles` instead of leaving as `is_deleted=true` ballast. Pre-flight verified `evidence_chunks_pmid_fkey` has `ON DELETE CASCADE` so parent DELETE auto-purges chunks; ran the existing batched orphan-chunks cleanup first (`scripts/openalex-bulk/cleanup-orphan-chunks.sql`, 410,188 chunks deleted in 18 iterations) so the cascade had nothing to do, then `scripts/centroid-llm/hard-delete-soft-deleted.sql` batched the parent DELETE in 10k iterations with pg_sleep(10) — 951,843 rows in ~18 min wall (pace held at ~50k/min throughout, near-instant DELETE since chunks were already gone, sleep dominated). Curated sources (pubmed/europepmc/elife/preprints/clinicaltrials/semantic-scholar) had zero soft-deleted rows and were untouched. **Final corpus**: 1,444,694 usable rows (openalex 470k + pubmed 418k + europepmc 257k + openaire 125k + clinicaltrials 53k + core 50k + semantic-scholar 47k + elife 14k + preprints 10k). Daily 05:00 UTC backup is the only restore path now (per-cull drop-list CSVs survived locally too). Followed by VACUUM ANALYZE on `research_articles` to reclaim ~951k worth of dead-tuple space; evidence_chunks autovacuum (scale_factor=0.05) handles its own backlog incrementally. — `scripts/centroid-llm/hard-delete-soft-deleted.sql` (new)

- 2026-04-23 (late) — **Borderline-zone LogReg filter dropped 197,220 of 263,535 papers (74.8%) in the 0.50–0.60 centroid-distance band.** Stage-2 of the corpus cleanup that started overnight with the >0.60 centroid cull. **Teacher signal**: ran GPT-5.4 (via Codex CLI on the user's ChatGPT Pro sub — no API spend) over 500 stratified borderline papers (5 bands × 100, 0.50–0.60 cosine distance from `fitness_v1` centroid), batched 50/req via `scripts/centroid-llm/codex-classify-batched.js` (~6 min total). Output: 45 YES / 455 NO at `scripts/centroid-llm/data/codex-labels.csv`. **Sanity**: 94% agreement with the 50 hand-labeled overlap (3 disagreements all defensible osteoporosis-treatment edge cases — Codex stricter, my hand label more permissive). **Embedding-classifier eval** (`scripts/centroid-llm/{train-classifier,knn-classifier}.js`, LOOCV): kNN k=3 hit 92.8% accuracy but 64% FN (kills useful research) due to class-imbalance majority bias; nearest-centroid 80.2% / 20% FN; LogReg λ=0.1 with 5.56× class weight on YES landed at 82.6% / 22% FN / 17% FP — best operational trade-off. LoRA Qwen3:8b on RTX 5080 considered but rejected — marginal gain vs 1-2 days Blackwell+unsloth setup. **Final model**: trained LogReg on all 500 labels via `train-final-logreg.js` (1500 epochs, in-sample 83.8%), pushed weights as a `vector(1536)` row in `corpus_centroids` (id `borderline_logreg_w`, bias in `notes::jsonb`). **Inference SQL** at `scripts/centroid-llm/logreg-borderline-filter.sql`: streaming per-chunk dot-product (avoids materializing 263k 1536-d mean embeddings — finished in 6 min instead of 13+ min for the materialize-first variant); writes to UNLOGGED `logreg_scores(pmid, min_dist, z)` so results survive cross-SSH. Validated against 500 codex labels: deployed 83.8% — exact match to in-sample, confirming the pgvector `<#>` sign-flip + `notes::jsonb->>'bias'` extraction are correct. **Spot-check**: top-10 most-confident NO are all animal nutrition / cell mechanism (Cyanidin Mg2+ rats, ω3-rats Caco2, FEBS Letters glucose transport); top-10 YES are sports injury epi (BJSM, JAAOS, JAMA, basketball, tennis). Boundary samples (z near 0) are mixed but reasonable. Scores live entirely in (-1, +1) — no extreme confidence on either side, which matches the "embeddings can't reliably split this zone" finding. **Applied**: UPDATE z ≤ 0 → 197,220 soft-deleted in 62s (aggregator kept 841,834 → 644,614 = -23.4%). **Drop list**: `scripts/centroid-llm/data/logreg-drops-2026-04-23.csv` (197,221 lines, 9.7 MB) — fully reversible via `restore-logreg-drops.sh` if quality regresses. Orphan-chunk cleanup running via the same `cleanup-orphan-chunks.sql` batched-DELETE pattern (25k chunks per iteration, pg_sleep(30) between batches, autovacuum reclaiming HNSW pages incrementally). Combined with the >0.60 cull (-169k) + non-English cull (-176k) + junk-types cull (-228k) earlier today, the aggregator corpus has shed ~770k off-topic rows in 24h. — `scripts/centroid-llm/{eval-prompts,fetch-embeddings,fetch-500-embeddings,prepare-labels,train-classifier,train-final-logreg,push-logreg-to-pg,knn-classifier,codex-classify-batched,check-codex-agreement,prep-lora-data,validate-deployed-weights}.{js,sh}` (new), `scripts/centroid-llm/{export-borderline-sample,knn-borderline-filter,logreg-borderline-filter,push-logreg}.sql` (new), `scripts/centroid-llm/{run-knn-filter,apply-logreg-drops,restore-logreg-drops,validate-deployed-weights}.sh` (new)

- 2026-04-23 — **Source-centric evidence retrieval (v4) shipped end-to-end + title-as-passage rendering bug fixed.** The "Why this answer?" reveal was showing redundant blockquotes that just repeated the source title (e.g. "Sugar and oral health" excerpt = title) because 1.53M `chunk_type='title'` rows in `evidence_chunks` (38% of corpus) were first-class evidence chunks — when ANN matched a title chunk, downstream consumers got `chunk_text = title`. Two-layer fix shipped in one PR: **(1) Server-side passage substitution** via new `match_evidence_chunks_v4` plpgsql RPC (parallel to v3, gated by `RETRIEVAL_USE_V4` env flag). Returns one row per source (DOI-deduped) with the best non-title chunk substituted in via LATERAL lookup on `evidence_chunks.pmid` — preference order abstract > full_text > abstract_conclusions > abstract_results > abstract_methods > abstract_background > abstract_other. When no non-title sibling exists, marks `is_title_only_match=true` and demotes to result tail. Wider candidate window (5×match_count vs v3's 3×) so dedupe has options. **(2) Client-side defense-in-depth** — new `shared/why-this-answer-helpers.js` exports `isTitleEquivalentExcerpt(excerpt, title)` (case/punctuation/whitespace insensitive, treats <40 trailing chars as title-equivalent). `WhyThisAnswer` (top-3 retrieved passages) and `SourcesFooter` (full source list) both filter title-equivalent excerpts to the back, backfill from the deduped tail until 3 substantive items render, and render an honest italic `"Title-only match — full text not available."` fallback in place of redundant blockquotes. Picks up the server-side `is_title_only_match` flag when v4 is on, falls back to client-side comparison when on v3. **Pipeline propagation**: `api/emersus/retrieveDatabaseEvidence.js` branches on the env flag (skips dedupByDoi when v4 — RPC already deduped per source), passes `matched_chunk_type` + `is_title_only_match` through enriched rows. `pipeline/retrieve.js normalizeVectorEvidenceRow` blanks `excerpt`/`summary`/`why_it_matters` when title-only so neither the LLM prompt nor the UI sees redundant title-as-passage. `pipeline/format-sources.js` ships both new fields in the SSE `done` event sources payload. **Eval harness** (new) at `scripts/eval/retrieval-eval.js` + 20-fixture seed at `scripts/eval/fixtures/retrieval.json` covering nutrition/training/supplementation/sleep/recovery topics. Computes recall@5, exclusion violations, mean top-3 similarity, `matched_title_count` (HNSW-level title hits), and `shown_as_title_count` (the user-facing metric: chunk_type==='title' post-substitution). Baselines saved to `scripts/eval/baselines/`. **Eval results, v3→v4**: `shown_as_title 167/200 (83.5%) → 0/200 (0.0%)` — every title-as-passage row eliminated. Recall@5 unchanged at 21/28 (0.0pp delta — v4 doesn't change WHICH papers retrieve, only WHAT CONTENT they show). Mean latency 685ms→393ms (43% faster, no client-side dedupByDoi pass). **Coordination context**: shipped after the corpus-centroid filter (which had soft-deleted 754k off-topic papers + dropped their chunks earlier today), so the original "Sugar and oral health" pmid 10004277354 was already gone; tested instead against pmid 10004375236 ("Sugar and metabolic health") which survived with title+abstract chunks intact — v4 substitution confirmed working on that case. Migration applied via stdin (per the `feedback_migration_scp_conflict.md` rule, no scp). v3 RPC stays callable as the rollback path; drop scheduled 2026-04-30. Phase 2 (BM25 title index for recall boost) priority dropped — centroid filter already covers most of what BM25 was designed to address. — `supabase/20260423_match_evidence_chunks_v4.sql` (new), `api/emersus/retrieveDatabaseEvidence.js`, `api/emersus/pipeline/retrieve.js`, `api/emersus/pipeline/format-sources.js`, `shared/react-chat-app.js` (WhyThisAnswer + SourcesFooter), `shared/chat.css` (.wta-excerpt-fallback + .srcs-snippet-fallback), `shared/why-this-answer-helpers.js` (new), `tests/unit/shared/why-this-answer.test.js` (new), `scripts/eval/{retrieval-eval.js,fixtures/retrieval.json,baselines/*.json}` (new), `docs/superpowers/specs/2026-04-22-evidence-retrieval-source-centric-design.md` (new), `docs/superpowers/plans/2026-04-22-evidence-retrieval-source-centric.md` (new)

- 2026-04-23 (overnight) — **Off-topic centroid filter** dropped 169,302 aggregator-source papers (openalex 115,491 + openaire 48,402 + core 5,409) whose chunks have MIN cosine distance > 0.60 to the new `fitness_v1` centroid. Built centroid as `avg(embedding)` over 146,855 chunks from 21 gold-standard fitness/nutrition journals (MSSE/JSCR/BJSM/AJCN/Nutrients/Sports Medicine/etc.); persisted in new `corpus_centroids` table. Catches OpenAlex ML-misclassification leaks (NAC chickpea plant biology, almond pollen germination, Biocycle composting, business management, nuclear engineering, anthropology) that survived all upstream filters. Then batch-DELETE'd 443,991 orphan chunks via the existing pg_sleep-paced cleanup script in ~11 min; per-table autovacuum now incrementally reclaiming HNSW pages in background. Reversible — drop list at `~/centroid-drops-2026-04-22.csv` on Hetzner (169,306 rows). Final usable per source: openalex 605,981 / openaire 179,193 / core 56,660. Smoke retrieval intact (top-result similarities 0.62–0.81 for protein / creatine / deload queries). Spec + plan + clean SQL files under `docs/superpowers/{specs,plans}/2026-04-22-corpus-centroid-filter*.md`. — `supabase/20260422_corpus_centroids.sql` (new), `scripts/build-fitness-centroid.sql` (new), `scripts/centroid-filter.sql` (new), spec + plan docs

- 2026-04-23 — **"Why this answer?" reveal + Save-to-library end-to-end + competitor-scan pause.** **WhyThisAnswer (hedged v1):** compact `<details>` disclosure under each assistant message exposing the top-3 retrieved evidence passages that backed the synthesis — title + journal·year + 2-sentence italic excerpt per item. No rerank/similarity scores surfaced; excerpts trimmed to ~220 chars at sentence boundary via `trimExcerpt()`; capped at top-3. Purely client-side — the excerpt payload was already in the SSE `done` event's `sources` via `formatSources()`. Sits above the existing `SourcesFooter`; both render together. Numbered circular markers, accent-border quote blocks, native `<details>` for a11y-correct keyboard + screen-reader behavior. Verified in light mode on the mockup at `app/why-this-answer-mockup.html`. **Save-to-library (Task 47) shipped:** migration `20260423_user_saved_sources.sql` with RLS (auth.uid()-scoped read/insert/update/delete + service_role bypass). Table stores `source_id` (DOI / pmid:N / openalex id), `saved_at`, `saved_from` (thread_id + message_id + thread_title), optional 500-char `note`, and a bibliographic-fact `meta_snapshot` JSONB (title/authors/journal/year/doi/pmid/source/url) as a fallback for papers later pruned from `research_articles`. **Copyright-clean by design** — we never duplicate abstracts or excerpts into per-user rows, just references + facts. 4 endpoints at `/api/emersus/saved-sources`: GET list (+ `?ids_only=true` hydration path for chat bookmark state), POST save, DELETE by id, DELETE by source_id (for the chat toggle which doesn't know the row uuid). Cap enforcement on POST via `resolveTier()` → `readTier()` (same cached 60s-TTL map as chat) — Free capped at 20, Pro uncapped. Over-cap returns 402 `library_full` with cap/tier/message; client shows a toast with an Upgrade CTA to `/pricing`. Mounted under `requireAuth` but NOT `userRateLimit()` (which would burn a daily-chat-message token on every bookmark). Unique `(user_id, source_id)` → 23505 unique_violation handled idempotently (returns existing row, not 500). **Bookmark icon** on each SourcesFooter row's `.mini-links` strip, next to PubMed/DOI links: hollow outline `aria-pressed="false"` when unsaved, filled `.is-saved` accent color when saved, optimistic toggle with rollback on error, SVG bookmark path. `savedSourceIds` Set hydrated once per session from `?ids_only=true`, threaded down through `Message` → `SourcesFooter`. **New `/app/library` page** (5.2 kB gzipped bundle): React + `createRoot` + `requireAuth`, list view with count pill (`3 / 20` free or bare count on pro), client-side search filter (title/authors/journal substring), 4-way sort (newest/oldest/title A-Z/year desc), per-item remove button, `saved_from.thread_id` deep-link back to originating thread, relative-time stamps, empty-state CTA → /app/, over-cap banner on free tier, mobile-responsive. Styled in new `shared/library.css`. **Nav integration:** "Library" link added to app-sidebar section list on chat, train, nutrition, progress, profile; regex updated for `is-active` highlighting. Registered in `vite.config.js` htmlEntries. **Pricing page updated:** Free column shows "Personal library · up to 20 saved sources"; Pro shows "Unlimited personal library · save every paper". **Deploys triggered emersus-api restart** (side-effect: heap reset, same pattern that accidentally fixed the "unable to generate recommendation" 500s earlier — which turned out NOT to be a memory leak; 344 restarts had stabilized hours prior and the heap gauge was misread). **Competitor scan** on request: Consensus is the only direct competitor (research-chat with DOIs, but cross-domain, no fitness product layer); RP Hypertrophy has brand+programming but no retrieval+citations; ChatGPT Health (Jan 7 2026) grounds in user's personal health data not scientific literature; HevyGPT is a ChatGPT Custom GPT, not a product. Emersus sits in a near-empty quadrant — fitness vertical × paper-grounded retrieval × interactive plan/widget × evidence grading. **iOS port brainstorm still paused** per user (see `project_ios_port_deferred.md`). — `supabase/20260423_user_saved_sources.sql` (new), `api/emersus/saved-sources.js` (new), `app/library/{index.html,library.js}` (new), `shared/library.css` (new), `shared/react-chat-app.js` (WhyThisAnswer + savedSourceIds state + bookmark + library toast), `shared/chat.css` (.why-this-answer + .save-toggle + .library-toast), `server.js` (mounts + import), `vite.config.js` (htmlEntries), `app/{nutrition,profile,progress,train}/index.html` (Library nav link), `pricing/index.html` (feature rows)

- 2026-04-22 (even later) — **Chat orb + empty-state + 21 UX shipped end-to-end.** **Orb:** rebuilt from scratch as a 3D particle cloud (`shared/emersus-orb/`) replacing `thinking-glyph.js`. 260 particles via `THREE.Points`+`ShaderMaterial` loaded from esm.sh — no bundler. Greedy nearest-neighbour target reassignment + underdamped spring + initial tangent velocity (curved gravitation) + per-particle stagger. 31 target shapes across polyhedra / topology (Möbius, Klein, torus knot, helicoid, catenoid, linked, supertoroid) / chaos (Lorenz, Rössler, Thomas, Halvorsen) / bio (DNA strands+rungs, molecule, seashell, heart, sunflower) / cosmic (galaxy, Saturn) / curves (Viviani, Lissajous, infinity). State machine: idle (frozen), thinking (frozen + breath pulse), responding (2 s random-no-repeat shape cycling). Per-shape intrinsic rotation axis+speed + per-state XYZ rotation lerps. Singleton canvas physically re-parented via DOM `appendChild` (no React remounts = particle state persists across slot changes). Rotating label beside orb in JetBrains Mono ("Searching 2.4M papers", "Cross-referencing studies", etc.) every 3.2 s. **Empty-state (ChatGPT/Claude-style):** composer `position: absolute; top: 50%` inside `.chat-main` (not `.chat-main-body` — composer is a sibling, not a child; found with Playwright using a live test account created via Supabase admin API). Welcome block anchored just above composer, horizontally centered. Removed "Ask about training…" subtitle. `is-empty-thread` triggers on "no user messages yet" (onboarding assistant greeting doesn't break the layout). 100-prompt rotating pool in `shared/chat/empty-prompts.js` cycles every 5 s with fade-in. **21 UX features:** draft auto-save per-thread (localStorage), keyboard shortcuts (⌘K/⌘//⌘[/⌘]/Esc/?) + help overlay, full-text thread search over message bodies, slash-command menu (/new /cite /share /summarize /help), time-aware welcome ("Morning"/"Evening"/"Welcome back — N days"), focus-ring redesign (2 px accent), arrow-key chip nav, submit-button state morph (idle/streaming/regenerate), pin up to 3 threads, undo-delete toast (6 s window), BibTeX/APA/plain citation formatters, voice input via Web Speech API (no backend), session-end summarize chip (10+ messages), first-turn celebration burst, mobile bottom-sheet composer (compact-then-expand on focus), swipe-left-to-delete thread (mobile), haptics on submit (6 ms) + response end (8 ms), live-region polish (announce once at stream end, no per-token stutter). Auto-scroll to bottom on response end when user has scrolled up. **Deferred — needs backend:** "Why this answer?" (exposure of retrieved chunks in SSE), Save-source-to-library (new `user_saved_sources` table + endpoints), file/link drop zone. Orb + empty-state + UX: ~50 commits, all auto-deployed via Hetzner webhook. — `shared/emersus-orb/*` (new module), `shared/react-chat-app.js`, `shared/chat.css`, `shared/chat/empty-prompts.js`, `shared/citation-format.js`, `app/emersus-orb-mockup.html` (new), `docs/superpowers/specs/2026-04-22-chat-orb-design.md` (new), `docs/superpowers/plans/2026-04-22-chat-orb-implementation.md` (new); deleted `shared/thinking-glyph.js`

- 2026-04-22 (late) — **English-only corpus enforcement** (defense-in-depth against non-English aggregator content). Added `research_articles.language` column (ISO 639-3) + franc batch detection across all 1,596,457 aggregator rows (openalex+openaire+core), ran in ~50 min on Hetzner with no API calls. Top non-English breakdown: ind 51.7k, spa 36.6k, fra 32.3k, por 24.4k, deu 20.8k, tur 9.3k, others. **Soft-deleted 175,695 non-English rows** (kept eng + sco — franc occasionally mis-tags terse English titles as Scots). Three-layer enforcement: (a) RPC `match_evidence_chunks_v3` filters `language IN ('eng','sco') OR language IS NULL` at retrieval; (b) per-topic `scripts/sources/openalex.js` adapter rejects `work.language !== 'en'` at `normalize()`; (c) bulk `scripts/openalex-bulk/filter.js` drops non-English at filter-time. Then **batched-DELETE'd 330,430 orphan chunks** in 15 iterations of 25k+sleep30s — finished in ~5 minutes with the new `vacuum_cost_limit=2000`/`cost_delay=0` tuning (vs the 13h grind earlier today). Autovacuum now running incrementally in background to reclaim HNSW index space. Final usable openalex 799,898 / openaire 228,602 / core 71,574. TrenMax / Indonesian sports-pedagogy / Russian Лесгафт / Turkish parenting tips physically cannot reach chat now. — `supabase/20260422_research_articles_language.sql` (new), `supabase/20260422_match_evidence_chunks_v3_language.sql` (new), `scripts/detect-language.js` (new), `scripts/sources/openalex.js`, `scripts/openalex-bulk/filter.js`, `scripts/openalex-bulk/cleanup-orphan-chunks.sql` (new), `package.json` (franc dep)

- 2026-04-22 — **OpenAlex bulk-ingest pipeline shipped + corpus quality cleanup.** End-to-end ~20h: filter 640 GB S3 snapshot on laptop (8h 22m, 2127 partitions, 492 M records → 1.16 M matches via OpenAlex 2024 Topics taxonomy + 20 curated topic allowlist), `scp` to Hetzner (491 MB), bulk INSERT in 4m 35s (1,124,744 new rows via batched-unnest + `ON CONFLICT DO NOTHING`), embed 1.38 M chunks in 11h 4m. **Mid-run blockers found and fixed**: (a) PG ran on bare defaults — bumped `shared_buffers 128MB→4GB`, `effective_cache_size→12GB`, `work_mem→32MB`, `maintenance_work_mem→1GB` + supabase-db restart; (b) pg-boss default `expireInSeconds=900` killed long handlers — re-enqueued with 12h expire + retryLimit 10; (c) `UPDATE pgboss.job` doesn't cancel handlers (zombies held 6 PG connections, hammered HNSW disk for hours) — must use `boss.cancel()`, `pg_terminate_backend()` if needed; (d) embed handler races caused 5x duplicate OpenAI calls — refactored to `SELECT … FOR UPDATE SKIP LOCKED` inside a transaction + new partial index `evidence_chunks(id) WHERE embedding IS NULL` (`commit a1a4e9bb`); (e) bumped embed-batch concurrency 1→4 (`commit 50ef1c21`). **Cleanup pass**: 228k OpenAlex rows soft-deleted as junk types (datasets, libguides, dissertations, books, letters, "other", reports, etc — none cite-worthy); 855k labeled `Journal Article`; 29k labeled `Review` + flipped `peer_reviewed=true` (was wrongly false); 11k labeled `Preprint`. Patched `jobs/ingest-openalex-bulk.js` to write these labels at insert (`commit 64e99636`). **Per-topic adapter sibling fix**: same SEO/PED-marketing leak hit pre-bulk per-topic ingests — caught "TrenMax Guide for Maximum Muscle Growth in 2026" as `type=report` slipping through to retrieval; soft-deleted 5,855 legacy junk-type rows + 12 explicit SEO patterns + patched `scripts/sources/openalex.js` to drop junk types at source (`commit 6dd318fb`). **Vacuum tuning**: 252k orphan-chunk DELETE triggered a 13h autovacuum on the 23 GB HNSW index (single-threaded, disk-bound, throttled by default cost_delay). Made permanent: cluster `vacuum_cost_limit=2000` + `autovacuum_vacuum_cost_delay=0`; per-table `evidence_chunks` `autovacuum_vacuum_scale_factor=0.05` so vacuums fire 4× more often on tiny batches. Smoke retrieval validated — OpenAlex now ranks in top-3 for protein/creatine/exercise queries (sims 0.62-0.67). Net usable corpus: ~895k OpenAlex rows + ~1.4 M existing. Cost: ~$25 OpenAI embeddings. — `scripts/openalex-bulk/filter.js` (new), `scripts/send-openalex-bulk.js` (new), `jobs/ingest-openalex-bulk.js` (new), `jobs/embed-batch.js`, `jobs/_registry.js`, `worker/index.js`, `scripts/sources/openalex.js`, `docs/openalex-bulk-plan.md`

- 2026-04-22 — Replaced the landing `#flow` section (4-step text grid) with three animated "Ask · Read · Connect" cards, unifying their visual language on dots. **Source material**: reverse-engineered Stripe's landing card animations — downloaded 73 JS chunks + 11 CSS files, found `index-0dff987f888fda2f.js` contains all card logic (Three.js + custom vertex shaders + `map_dots.png` Poisson sampling for 30k-60k globe dots). **Card 1 (Ask)**: port of Stripe's `agentic-graphic` vertex shader — 260 paper-dots on an oval annulus, each with a radial outward direction from centre; per-frame we apply two sin-noise layers for drift, compute a `thinkingVector` (unit-vector centre→cursor, hovered-only), and amplify outward displacement along dots whose radial direction aligns with the cursor (`align³ * hoverStrength`). Dots within 16% of the cursor snap back to rest. Fixed the old `translate(%, %)` bug where 1.03% on a 3 px dot = 0.03 px (invisible); now `translate3d(Npx)` with SCATTER=14 + THINK_BOOST=22. **Card 2 (Read)** went through three iterations before landing: (1) Lissajous spotlight over a blurred paper wall — invisible through the blur; (2) horizontal scan beam + mint hairline — user rejected; (3) continuous content reel scrolling methodology text — visible but didn't fit visual language. Final: **reading field** — tilted 26×14 grid of cell-papers (`rotateX(48deg) rotateZ(-6deg)`), a reading wave sweeps columns 0→1 over WAVE_DURATION=7.5s, per-cell state derived from `|diff|` to wave front (live→mint+glow+translateZ(14px), warm→azure+mild glow+translateZ(6px), read→pale mint flat); prominent counter panel at top ticks `1,284,679 / 2,000,000` at 640 papers/s with a `sin(wave*2π)*0.9` boost multiplier peaking at the front, plus mint progress bar + pulsing live-dot. **Card 3 (Connect)**: true 3D dot-sphere in canvas — started as SVG flat projection tilting on hover (user rejected as "flat painted dots"); rebuilt with Fibonacci lattice sampling of 820 points on a unit sphere, per-frame Y-rotation (continuous, 0.085 rad/s) + X/Y hover tilt, perspective divide with camera distance 2.4, depth fade `0.28 + depth^1.3 * 0.72`, back-to-front sort; per-dot corona bursts port `money-movement` shader's cycle (launch/fade/idle phases with varianceScale, 9–18s interval/dot, `sin(wave*π)` envelope up to +22% outward radius) + per-dot sinusoidal twinkle. BMJ/JAMA/Nature labels are 3D-anchored to front-hemisphere unit vectors and reprojected each frame using the hover rotation only (not the spin) so they follow the cursor-driven motion without orbiting out of view. Canvas is DPR-aware with ResizeObserver. **Integration**: extracted mockup CSS via a Python scoping pass (prefixes every top-level selector with `.ark-how`, drops `:root`/`html`/`body` resets, remaps `--bg-card`/`--border`/`--accent` to landing's `--surface`/`--line`/`--wave-2` tokens) into `shared/ark-how.css`; JS into `shared/ark-how.js` wrapped in a single outer IIFE. Fix: had to change `<script src>` → `<script type="module" src>` because Vite's MPA build can't bundle non-module scripts and was leaving a 404 at `/shared/ark-how.js` in prod. Section max-width bumped to 1260 just for `.ark-how` (landing's `.section` default 1100 squeezed the three-card row). Counter `.num` shrunk 32→26px at `section.ark-how` specificity so "/ 2,000,000" fits the narrower landing column. Static A/B/C brainstorm mockups at `app/card2-options-mockup.html` (funnel / reading field / stack-to-claim) — user picked B. — `index.html`, `shared/ark-how.css`, `shared/ark-how.js`, `app/emersus-cards-mockup.html`, `app/card2-options-mockup.html`

- 2026-04-21 (late) — Linear-style atmospheric lighting on landing (dark palette only), plus the cascade of polish it forced. Reverse-engineered Linear's `page_glow` + `page_shine` + `PillarHero_title::before` + `Frame_background` rules from their Next.js CSS; cursor-tracked spotlight border via `mask-image: radial-gradient(at var(--mask-x) var(--mask-y))` attached to `.bento-card`/`.roadmap-card`/`.stat`/`.step`/`.faq-item` (200px proximity field, `--lfx-prox` cross-fades idle → follow); corner ambient glows with `mix-blend-mode: lighten`; body::after rewritten as sun-below-fold + L/R rim lights via `color-mix()`; SVG-turbulence grain overlay at 22% opacity with overlay blend; soft text glow behind `h1.headline`. All rules scoped under `:root[data-theme="mint"]` so cream Paper palette is untouched (cream absorbs colored glows at low opacity — Linear's aesthetic needs darkness to register). **Seam fix**: new body::after glow exposed a horizontal seam where `.hero-bg::after` faded to opaque `var(--bg)` at the hero band's bottom; switched the bottom stop to transparent so glow flows through continuously. **Grid mesh removed** (`body.landing-body::before { display: none }`). **Health disclaimer relocated** from footer-bot base into the footer brand column under the tagline, smaller font, no border-top, left-aligned, max-width 44ch. **Card hover unification**: six card types had 3-4 overlapping hover effects (legacy static `::before` top-right radial + new cursor spotlight + border-or-background shift); collapsed to cursor spotlight + border-color change. `.step` + `.stat` swapped from bg-shift to inset 1px border-shift via `box-shadow: inset 0 0 0 1px var(--accent-line)` matching `.bento-card`/`.roadmap-card`/`.faq-item`. `.paper-card` previews intentionally static (content inside another card, not click targets). **Evidence bento expanded**: `.bento-card.tall` Evidence card had ~300px of dead space above the 4 thin grade bars; `.scale-row` now stacks `bar head + short description line` full-width with trimmed copy per grade (HIGH/MODERATE/LIMITED/INSUFFICIENT). **Em-dash sweep**: all landing-copy em-dashes replaced — titles use `·`, mid-sentence use `.`/`,`/`:` (code-comment dashes left). **Lenis smooth-scroll**: wheel/trackpad momentum via esm.sh (`duration: 1.15s`, expo-out easing). Disabled for `prefers-reduced-motion` + `pointer: coarse`. Anchor links intercepted for `lenis.scrollTo` with 80px nav offset; `scroll-padding-top: 80px` on html as native fallback. **FAQ animation rewrite**: previous code measured `scrollHeight` *before* setting `padding-bottom: 20px`, so target `max-height` was 20px short → animation smooth until content edge, then visible snap when `afterMaxHeight` set `max-height: none`. Fix: disable transitions while measuring so target includes final padding; replaced `setTimeout(DURATION)` with `transitionend` + 600ms safety timeout; reentrancy guarded by `busy` flag; prefers-reduced-motion falls back to native. Backups of pre-change files at `backups/landing-2026-04-21-pre-linear/{index.html,shared/landing.css}`. — `index.html`, `shared/landing.css`

- 2026-04-21 — Ingestion-pipeline overhaul + 3 new sources + landing-page live stats. **Discovered the corpus had stalled for 9 days** (last ingest Apr 12) because no `ingest-topic` was on a cron — bulk-ingest only ran when an operator manually invoked `scripts/fill-pmc-topics.js`. Added `bulk-ingest-active-topics` weekly schedule (Sun 02:00 ET) that mirrors the manual fanout. **Diagnosed 132 biorxiv/medrxiv failures** as a thundering-herd hitting a flaky api.biorxiv.org PHP backend (287 jobs × 2 sources × identical date-range URLs); restructured into a single `ingest-preprints-sweep` weekly job (Sat 06:00 ET) that walks each preprint source ONCE with broad exercise-science union filter, DOI dedup. Added `_http.js` JSON salvage (api.biorxiv.org leaks ~35 KB of PHP `<br /><b>Warning:` before the JSON; we now scan for the first `{` or `[` and re-parse, throwing typed `SourceTransientError` on hard failures so retries work cleanly). Added page-skip resilience to biorxiv/medrxiv/sportrxiv/psyarxiv adapters (one bad page no longer kills the whole iteration). Watermark optimization on the sweep handler: `daysBack = days_since(max(publication_date)) + 3` floored at 14 → 25× fewer requests on steady-state runs (~104s instead of 30+ min). Page timeout 45s → 15s on biorxiv/medrxiv (faster failure when upstream is degraded). **Three new source adapters live**: `clinicaltrials` (per-topic, v2 REST API, 50 trials in 9s smoke), `psyarxiv` (sweep mode, OSF backend, plugs sport-psych/motivation/adherence/body-image gap), `elife` (per-topic, vnd.elife.search+json header, 48 papers in 22s smoke). Probed PEDro / F1000Research / ChemRxiv / ResearchSquare / WHO ICTRP — all skipped after probing (PEDro requires data-sharing partnership via broken contact form, F1000 endpoints 404 + already in PubMed, ChemRxiv blocked by Cloudflare, ResearchSquare's public `/api/search` isn't real search, WHO ICTRP requires SharePoint access form). Schema CHECK constraint on `research_articles.source` extended for new sources via direct ALTER on prod. **OpenAlex throttled** 8→3 RPS to stay under free-tier $1/day budget after 158 jobs got rate-limited mid-fanout; 158 doomed jobs cancelled. **Landing-page corpus stats now live** — `/api/config` queries `pg_class.reltuples` for paper count + `count(*)` of active topics + `GROUP BY source` per-source breakdown, cached in-process for 5 min; `index.html` fetches on load and updates `data-count-to` on the count-up animations before the IntersectionObserver fires. Hardcoded baselines (1,041,448 / 302) replaced by live values (1,201,252 / 303). **Dead-code cleanup**: removed crossref/doaj/epistemonikos adapters (574 LOC; deprioritized/disabled, zero rows in prod, dead weight) including their tests, fixtures, side-effect imports, and the now-superfluous `DEPRIORITIZED_SOURCE_IDS` constant. **OpenAlex bulk-ingest plan written** for a future session at `docs/openalex-bulk-plan.md` (deferred — user picked the local-download approach over the $10/mo Premium upgrade; full handoff covering S3 snapshot facts, 3-machine architecture, dedup, schema mapping, cost estimates). Also: a detached watcher process at `~/scripts/trigger-backfill-elife-ct.js` waits for the morning's bulk-ingest to drain and then enqueues 303 `ingest-topic` jobs targeting clinicaltrials + elife only (priority=10) so the new sources backfill across all topics without waiting for next Sunday's auto-run. — `jobs/bulk-ingest-active-topics.js`, `jobs/ingest-preprints-sweep.js`, `jobs/_registry.js`, `jobs/ingest-topic.js`, `scripts/sources/_http.js`, `scripts/sources/{biorxiv,medrxiv,psyarxiv,sportrxiv}.js`, `scripts/sources/{clinicaltrials,psyarxiv,elife}.js`, `scripts/sources/openalex.js`, `api/config.js`, `index.html`, `tests/unit/jobs/ingest-topic.test.js`

- 2026-04-17 — Widget-v2 spec-complete: 42 new templates shipped (Plans 10a-10f) taking the catalogue from 13 → 55/55 across all 6 families. **Pharma** added supplement_stack_schedule, loading_vs_maintenance, absorption_multi_protein, effect_duration_strip, dose_threshold_band. **Training** added mev_mrv_range, rpe_histogram, rep_scheme_grid, training_stress_balance, fatigue_readiness_composite, weekly_plan_calendar, deload_protocol. **Nutrition** added food_nutrient_scatter, hydration_timeline, micronutrient_radar, calorie_balance_ledger, meal_timing_strip, tdee_waterfall, macro_ring_nutrition. **Evidence** added forest_plot (with pooled diamond), evidence_strength_card (GRADE badges), butterfly_comparison, study_quality_matrix, meta_regression_line, ci_ladder, citation_timeline, study_beeswarm. **Progress** added lift_progress_grid, weekly_volume_trend, adherence_calendar_heatmap, body_comp_trend, goal_trajectory_dual, intervention_slopegraph, session_consistency_strip, vo2max_trend, sleep_consistency_bars, pr_celebration_card, streak_counter_card, plus pr_progression_line alias. **Calculator** added macro_calculator, plate_loader_visual, rpe_to_percent_rm, body_fat_estimator, carb_cycling_calculator, protein_target_calculator, pace_calculator. All six family tools stay on strict:true + superset-data; each new template contributes its fields to the family's single data shape as nullable, disambiguated fields where collisions exist. Field renames in this pass: progress volume_trend `points` → `trend_points`, body_comp_trend `points` → `comp_points`, vo2max_trend `points` → `vo2_points`, evidence forest_plot `studies` → `fp_studies`, study_quality_matrix `studies` → `quality_studies`, citation_timeline `studies` → `timeline_studies`, study_beeswarm `dots` → `beeswarm_dots`, meta_regression_line `points` → `regression_points`, ci_ladder `protocols` → `ladder_protocols`, weekly_volume_trend `by_muscle` map → `muscle_sets` array (strict mode rejects object-with-dynamic-keys). Strict-mode preflight caught two late schema issues: nested object `required[]` missing under progress's `by_muscle` map, fixed by pivoting to array-of-entries. Comprehensive healthcheck green post-deploy: 6/6 schemas accepted, 12/12 per-template emission+validator passes, 0% prod validator drop across 34 emissions × 12 distinct types in the last 24 h. 50 new unit tests added (742 total, 18 still ECONNREFUSED-on-local-postgres — pre-existing). Remaining widget-v2 backlog: interactive sliders (Slider primitive exists, unused), legacy `emit_widget` deprecation soak (runbook in docs/widget-v2-rollout.md), and organic-traffic trigger-phrase tuning based on widget_v2_emission_events rollups. — `api/emersus/pipeline/tools.js`, `shared/widget-v2/validators/{pharma,training,nutrition,evidence,progress,calculator}.js`, `shared/widget-v2/templates/**/*.js` (42 new files), `shared/widget-v2/dispatcher.js`, `shared/widget-v2/tokens.css`, `tests/unit/shared/widget-v2/templates/**/*-new.test.js` (6 new files)

- 2026-04-17 — Widget-v2 template system shipped end-to-end (Plans 1-9.5). Replaces raw-HTML `emit_widget` tool-calls with six family-scoped structured tools (`emit_pharma_widget` · `emit_training_widget` · `emit_nutrition_widget` · `emit_progress_widget` · `emit_evidence_widget` · `emit_calculator_widget`) emitting typed JSON that renders inline React components. 13 templates shipped across the 6 families: calculator (macro_ring, tdee_calculator, one_rm_estimator), nutrition (protein_distribution_bar, meal_macro_stack), training (periodization_ladder, volume_intensity_grid), progress (pr_timeline, volume_trend), pharma (dose_response_curve, half_life_decay), evidence (study_matrix, effect_size_forest). **Architectural inflection point:** all multi-type tools use `strict: true` + a superset-data schema — every field from every variant listed in `required[]` + nullable at the type level; `type`-specific fields populated, others set to `null` by the model. This landed after a full-day blind alley where `strict: false` was tried first for schema flexibility and failed: under `strict: false` OpenAI treats `required[]` as advisory prose and `gpt-5.4-mini` reliably omits `data` entirely even with "YOU MUST" copy + JSON examples in the description. Field-name collisions resolved by renaming: nutrition's `meals` → `protein_meals` / `macro_meals` (different item shapes per variant); training's grid `weeks: int[]` → `grid_weeks` (didn't collide with periodization's `weeks: int`). Pipeline wiring: `stream.js` processes `response.output_item.done` for every `emit_*_widget` name, re-validates server-side via per-family validators, forwards valid payloads via SSE `tool` events; dispatcher in `shared/widget-v2/dispatcher.js` routes `family.type → component`; unknown types render a red diagnostic card. Telemetry: `widget_v2_emission_events` row per emission with `validator_result`, `elapsed_ms`, `output_tokens` (populated via a post-stream flush because `response.completed` fires after `onTool`). Feature flag `WIDGET_V2_ENABLED=true` active on prod. `WIDGET_V2_TOOL_TO_FAMILY` map in `shared/react-chat-app.js` routes SSE events to the React dispatcher. System prompt updated with per-family TRIGGER PHRASES + "YOU MUST CALL" framing. Inline chip click uses the existing `emersus:seed-prompt` CustomEvent bridge (widget-v2 widgets are not iframed, so `window.sendPrompt` — defined only inside legacy widget iframes — was unreachable). Pre-existing `emit_meal_plan` `fdc_id: null` validator drop fixed in passing. Along the way: 7 UTF-8 mojibake strings repaired (em-dash / middot / ellipsis / multiply / right-arrow) across meal-plan widget, chat text block, share-modal spinner, cardio, climb, swim, and session views. Olive-green `--color-accent-primary` / `--color-text-inverse` fallback hex cleared from MealPlanCard inline styles (legacy tokens not bridged in design-tokens.css → visible olive chip in Paper palette). Three verification scripts shipped: `scripts/widget-v2-family-preflight.js` (proves each family's schema is strict-mode-accepted with a representative prompt), `scripts/widget-v2-healthcheck.js` (layer-1 schema × 6 + layer-2 emission × 12 + layer-3 24h telemetry rollup; one command, ~60s), `scripts/widget-v2-emission-recall.js` (50-prompt natural-language emission-recall benchmark, one-off). Final recall benchmark: 90% recall (36/40 widgetable prompts fire correctly), 92% family-precision, 10% false-fire — above all target bands. Prod telemetry: 24h rolling rollup shows 0% validator drop across all families. `supabase/20260417_widget_v2_emission_events.sql` applied via `cat | ssh docker exec -i supabase-db psql -U supabase_admin`. Deferred to future sessions: legacy `emit_widget` deprecation (soak + warn + cut per runbook), remaining 41 templates from the original 54-template spec, interactive client-side sliders for `tdee_calculator` / `macro_calculator` (`Slider` primitive exists, not yet used). — `api/emersus/pipeline/{tools.js,stream.js,prompt.js}`, `shared/widget-v2/**`, `shared/react-chat-app.js`, `shared/emersus-renderer.js`, `shared/meal-plan-widget.js`, `shared/share-modal.js`, `app/workout/{cardio,climb,swim,session}/*.js`, `shared/meal-plan-schema.js`, `shared/widget-v2/tokens.css`, `supabase/20260417_widget_v2_emission_events.sql`, `scripts/widget-v2-{family-preflight,healthcheck,emission-recall}.js`, `shared/feature-flags.js`, `tests/**/widget-v2/**`
- 2026-04-17 — Anonymous ask endpoint + landing-page takeover (PR `feat/landing-ask-bar`). Public `POST /api/emersus/anon-ask` streams SSE answers without auth, gated by a 3-questions-per-24-h IP rate limiter (in-memory store, decrements on mid-stream abort). Landing page's rotation demo extracted to `shared/landing-demo.js` with a `takeover()` hook; first composer click wipes the rotating demo and hands off to `shared/landing-chat-runtime.js`, a vanilla-JS SSE chat runtime that persists per-day state in localStorage, renders prose deltas + sources + widget-v2 placeholder chips (sign-up prompt for anon users), and surfaces a block card on hard-refusal. LIVE pill added to the demo chrome. No-auth API path lets prospective users experience the product without signup friction while still hitting the production pipeline. — `api/emersus/{anon-ask,rate-limit}.js`, `server.js`, `shared/{landing-demo,landing-chat-runtime}.js`, `shared/landing.css`, `index.html`, `tests/unit/api/emersus/{anon-ask,anon-rate-limit}.test.js`


- 2026-04-16 — Progress charts — 4 chart types replace coming-soon stubs on /app/progress/: Momentum Cards, Beeswarm, Zone River, Control Chart · all theme-aware, all mobile-responsive · api/emersus/progress.js, shared/progress-charts.js, shared/progress-v2.css, app/progress/progress-v2.js, shared/design-tokens.css

- 2026-04-15 — Onboarding profile-update fences → function-calling — replaced ~~~profile-update regex parsing with update_user_profile OpenAI tool call; multi-turn resolution via previous_response_id — api/emersus/pipeline/onboarding.js, api/emersus/pipeline/tools.js, api/emersus/pipeline/stream.js
- 2026-04-15 — Meal-edit modal on /app/nutrition/ — tap a meal row to view/edit/delete items, add foods via search, auto-save amounts, change meal slots, standalone Log food button — app/nutrition/nutrition-v2.js, shared/nutrition-v2.css
- 2026-04-15 — Profile data moved behind `get_user_profile` server-side tool call. Profile no longer appears in the initial LLM prompt — the model calls a tool when it needs personalization (workout plans, meal plans, injury-aware advice), the server intercepts the call, injects the stored profile via `previous_response_id` follow-up, and the model continues generating. Prevents the model from randomly echoing/narrating profile fields on general knowledge questions. System prompt updated with explicit "do not surface profile data" instructions. — `api/emersus/pipeline/tools.js`, `api/emersus/pipeline/prompt.js`, `api/emersus/pipeline/stream.js`, `api/emersus/pipeline/synthesize.js`
- 2026-04-15 — History inline-expand on /app/train/ — click a session to see all exercises + set tiles with RPE color coding, top set highlight, accordion behavior — app/train/train.js, shared/train-v2.css, shared/design-tokens.css
- 2026-04-15 — `research_articles` canonical dedupe — schema + backfill + unique constraint. Ingestion adapters wrote the same paper multiple times under different `(source, external_id)` tuples: Zenodo versioned DOIs from OpenAlex created N rows for an N-version upload; cross-adapter ingests (OpenAIRE + OpenAlex + semantic-scholar + core) all wrote their own row for the same paper. Retrieval-side two-pass dedupe (commit `08d61e67`) masked the UX but the data layer still carried the waste (extra embeddings, slower retrieval, inflated topic-coverage counts). Three migrations applied to prod Hetzner Postgres in order: (1) `supabase/20260415_research_articles_canonical_dedup.sql` — added a STORED generated column `canonical_dedup_key = lower(title) | first-author-surname | year`, plus `md5(canonical_dedup_key)` and `lower(doi)` indexes (raw-string btree fails on long titles at Postgres's 2704 B per-row index limit). Safe; no data changes. (2) `supabase/20260415_research_articles_dedup_backfill.sql` — window-function `ROW_NUMBER() PARTITION BY md5(key) ORDER BY (doi IS NOT NULL) DESC, rcr DESC, citation_count DESC, updated_at DESC, pmid ASC`, deleted `rn > 1`. `evidence_chunks.pmid_fkey ON DELETE CASCADE` reclaimed the orphaned chunks automatically. Deleted **24,172** rows (~2 % of the 1.19 M corpus). (3) `supabase/20260415_research_articles_dedup_unique.sql` — took `LOCK TABLE ... IN ACCESS EXCLUSIVE MODE` (concurrent ingestion had slipped fresh dupes in between the backfill and the constraint build on the first attempt), re-ran the dedupe pass (121 more rows), dropped the non-unique index, and created `research_articles_canonical_dedup_key_unq UNIQUE INDEX ON research_articles (md5(canonical_dedup_key)) WHERE title IS NOT NULL AND publication_year IS NOT NULL`. Existing `ON CONFLICT DO NOTHING` in `jobs/ingest-topic-from-source.js` now catches composite-key collisions on future ingests — no JS change needed. Final state: 1,175,763 articles (down from 1,190,990), 2,733,033 chunks, 0 leftover dupe clusters. — `supabase/20260415_research_articles_canonical_dedup.sql`, `supabase/20260415_research_articles_dedup_backfill.sql`, `supabase/20260415_research_articles_dedup_unique.sql`
- 2026-04-15 — Content-aware skeleton loaders with left→right shimmer across every React app page. Replaces the flat "Loading…" text that sat in the gap between HTML paint and React hydration (typically 300-800 ms on cold loads with the `/api/config` → Supabase → page-endpoint chain). New `shared/skeleton.css` defines the primitive set: `.skel` base with a 1.4 s linear shimmer keyframe, `.skel-line` (12/16/22 px heights) with `.w-{10..100}` width modifiers, `.skel-block`/`.skel-circle`/`.skel-pill` with size variants, and `.skel-stack`/`.skel-row` layout helpers. Palette-aware via `--skel-base` + `--skel-hi` tokens set on `:root[data-theme="mint|paper"]`. `prefers-reduced-motion` swaps the shimmer for a static opacity pulse. Per-page skeletons re-use the real DOM classes (`.tr-modality-tabs`, `.pg-pr-grid`, `.message.assistant`, `.pf-header`, etc.) so when the real content swaps in there's no layout jump. Wired into: chat (`ChatThreadSkeleton` replaces the hydrating-thread welcome block with 4 alternating message shapes); train (`TrainSkeleton` for session boot + `TrainHistorySkeleton` for the History tab); nutrition (`NutritionSkeleton` + `NutritionTodaySkeleton` covers macro rings, water/supps strip, meal rows); progress (`ProgressSkeleton` + `ProgressBodySkeleton` for the PR grid, streak, and session list); profile (`ProfileSkeleton` for header + 6 tabs + 3 form sections). Loaded via `<link rel="stylesheet" href="/shared/skeleton.css">` on all 5 authenticated app HTML entry points. Auth + landing + legal pages render form/content from static HTML and don't need skeletons. — `shared/skeleton.css`, `app/index.html`, `app/train/index.html`, `app/nutrition/index.html`, `app/progress/index.html`, `app/profile/index.html`, `shared/react-chat-app.js`, `app/train/train.js`, `app/nutrition/nutrition-v2.js`, `app/progress/progress-v2.js`, `app/profile/profile.js`
- 2026-04-15 — Theme default flipped to Paper·Royal (light) + toggle moved from nav/sidebar to Settings (commits `cac7ed7d`, `c129fde0`). Rationale: product ships light-first; the universal banner/sidebar widget was user-hostile visual chrome, and `prefers-color-scheme` was dragging users into dark mode without opt-in. (A) `scripts/inject-no-flash-boot.js` now writes a boot block that returns `saved → 'paper'` (no `matchMedia` branch) and added a `--force` flag so existing sentinel blocks get re-injected. Ran `node scripts/inject-no-flash-boot.js --force` to update all 31 HTML entry points. `shared/theme.js` `resolveInitialTheme({ saved })` simplified to the same fallthrough (unit tests updated + pass). Existing users with `emersus-theme=mint` in localStorage keep dark until they toggle; only new/fresh-browser visitors see the change. (B) Removed `.theme-switch` pair from `index.html` landing nav and `.chat-side-palette` block from `shared/react-chat-app.js` (plus unused `paletteRef` + `bindSwitcher` import). Added a new "Appearance" tab in `app/profile/profile.js` with Light / Dark radio cards — swatch preview, accent-ring active state, `applyTheme(...)` on click. New `.pf-appearance-*` styles in `shared/profile-v2.css`. Static legal pages (`/privacy`, `/terms`, `/contact`, `/consumer-health-data`) keep their floating `palette-switch` widget since public visitors have no profile entry point. — `scripts/inject-no-flash-boot.js`, `shared/theme.js`, `tests/unit/shared/theme.test.js`, `index.html`, `shared/react-chat-app.js`, `app/profile/profile.js`, `shared/profile-v2.css`, 31 HTML entry points re-injected
- 2026-04-15 — Mobile polish — Phase C (commits `ee0692ab`, `ef4185e0`, `20fd510a`, `36414ee6`). (C1) Touch targets bumped to Apple HIG 44×44 min at `≤ 768 px`: `.submit-orb` 36→44, `.srcs-chip` 26→32, `.pill`/`.msg-action` min-height 32, `.stop-btn` 40×88, `.srcs-row .mini-link` min-height 32. (C2) Message bubble padding steps down 32→20→16 px at 640/400 breakpoints; `.msg-body` font drops to 14.5 px at ≤ 400 px; `.thread-welcome-title` 22 px + copy 14 px at ≤ 500 px; `.empty-prompts-row` goes flex-column at ≤ 400 px with 100 %-width chips. (C3) Widget iframe clamps to viewport: `maxWidth: "100%"` on the React iframe style object + `max-width: 100vw; overflow-x: hidden` on `html, body` inside `EMERSUS_THEME_CSS`. (C4) Auth form inputs `font-size: 16 px` at `≤ 640 px` to suppress iOS focus-zoom (desktop stays 14 px). Spec: `docs/superpowers/specs/2026-04-15-mobile-polish-design.md`. Plan: `docs/superpowers/plans/2026-04-15-mobile-polish.md`. — `shared/chat-v2.css`, `shared/emersus-renderer.js`, `shared/auth-v2.css`
- 2026-04-15 — Mobile chrome fixes — Phase B (commits `996e5cae`, `ef34a2dc`, `46cf097f`, `15479edc`). (B1) `env(safe-area-inset-*)` applied to `.top-bar` (`shared/chrome.css`) and `.tr-bottom-bar` (`shared/train-v2.css`) so iPhone notch + home-indicator don't clip content; desktop unchanged via `max(N, env(...))` fallthrough. (B2) `.tabs` now `overflow-x: auto` with hidden scrollbar + `scroll-snap-type: x mandatory` at `≤ 640 px`, fixing Train modality tabs (and all other shared-chrome tabs) at 375 px. (B3) Chat top-bar pills collapse to icon-only at `≤ 500 px` — added `<span class="model-pill-label">`, `<span class="pill-sources-label">`, `<span class="share-btn-label">` wrappers in `shared/chat/top-bar.js` so mobile CSS can hide the text while keeping chevrons/counts/dots visible; title ellipsizes at 55 vw. (B4) `body { overflow-x: hidden }` defensive belt added to `train-v2`, `nutrition-v2`, `progress-v2`, `profile-v2` so rogue inline widths can't create horizontal scrollbars. — `shared/chrome.css`, `shared/chat-v2.css`, `shared/train-v2.css`, `shared/nutrition-v2.css`, `shared/progress-v2.css`, `shared/profile-v2.css`, `shared/chat/top-bar.js`
- 2026-04-15 — Mobile chat fixes — Phase A (commits `9e43ba7f`, `5f807d3c`, `56b8dd11`, `94755861`, `1596b44d`). Three critical issues that blocked usable `/app` on phones: (1) `height: 100vh` on `.app-shell` (`shared/chrome.css:43`) and `.chat-app-shell` (`shared/chat-v2.css`) replaced with `100dvh` + `100vh` fallback so iOS Safari address-bar retraction and software keyboard raise don't collapse the grid; (2) 280 px sidebar transformed into an overlay drawer at `@media (max-width: 768px)` — slides in from left over a 45 % black scrim, Esc/tap-outside/tap-thread close it, body scroll-locked while open via `html.is-nav-open`; 44×44 hamburger trigger added to the top bar left of the title; (3) composer keyboard guard — chat thread gets `padding-bottom: calc(140px + env(safe-area-inset-bottom))` under 768 px so messages clear the fixed composer, and composer wrapper pads the iOS home-indicator zone via `max(28px, env(safe-area-inset-bottom))`. Zero desktop regression (≥ 769 px renders byte-identical). Spec: `docs/superpowers/specs/2026-04-15-mobile-polish-design.md`. Plan: `docs/superpowers/plans/2026-04-15-mobile-polish.md`. — `shared/chat-v2.css`, `shared/chrome.css`, `shared/react-chat-app.js`, `shared/chat/top-bar.js`
- 2026-04-15 — LLM-generated chat titles. Sidebar titles were previously the first user message truncated to 42 chars ("How much protein should I e..."). Added `api/emersus/thread-title.js` — cheap one-shot OpenAI call (model via `OPENAI_EMERSUS_TITLE_MODEL` → parser model → `gpt-5.4-mini` chain) that takes `{ question, answer }` and returns a 3-6-word topical title (max 48 chars, plain sentence case, no quotes/period/emoji, normalized server-side). Mounted at `POST /api/emersus/thread-title` behind `requireAuth`. Client wiring in `shared/react-chat-app.js`: `submitQuestion` fires `maybeGenerateThreadTitle` after the first `persistThread` completes (guarded by `messages.length === 2`); the helper is thread-id-scoped (not bound to `activeThreadId`) so it renames the correct thread even if the user has switched tabs. Also split `deriveThreadTitle` into `deriveDefaultThreadTitle` + a preserving wrapper — any non-placeholder existing title (LLM-generated OR manual rename via the top-bar) now survives subsequent `persistThread` calls instead of being overwritten from the first user message. Latent rename-overwrite bug closed along the way. — `api/emersus/thread-title.js`, `server.js`, `shared/react-chat-app.js`
- 2026-04-15 — Dedicated chart series palette (no more "two blues" charts). Old prompt rotated chart data colors through `var(--accent-primary)`, `var(--color-text-info)`, etc. In Paper·Royal that's `#3b82f6` and `#2563eb` — both blue, indistinguishable on a 2-series line chart. Added 5-entry categorical palettes (`--chart-series-1..5`) per theme, tuned for perceptual separation: mint → jade / sky / amber / red / violet; paper → royal / emerald / orange / red / purple. Chart.js configs can't resolve CSS vars, so the resolved hex values are also exposed as `window.EMERSUS_CHART_SERIES[0..4]` inside every widget iframe (re-resolved on theme flip when srcDoc reloads). Both prompts (`prompt.js` + `tools.js`) updated to route chart data through these, explicitly forbid `--accent-primary` / semantic `success/warning/danger/info` for data series, and document the `+ '22'` hex-alpha trick for fills. — `shared/emersus-renderer.js`, `api/emersus/pipeline/prompt.js`, `api/emersus/pipeline/tools.js`
- 2026-04-15 — Widgets readable in light mode. The palette-aware iframe CSS from earlier today only helped when the widget HTML used CSS vars; the LLM system prompt in `api/emersus/pipeline/prompt.js` and `tools.js` still told the model "Dark surface (#0c0e11), off-white text (#f9f9fd)" + hardcoded `rgba(255,255,255,0.55)` chart axis + legacy `#6d9fff/#9ffb00` accent hex. So every generated widget inline-styled white text that no CSS token remap could override. Fixed in two passes: (A) rewrote both prompts to drop all hardcoded hex, tell the model there are two palettes that flip at runtime, and require `var(--color-*)` / `var(--accent-*)` / `var(--chart-*)` exclusively — including a no-override rule for Chart.js `ticks.color` / `grid.color` / `borderColor` since the iframe already bakes palette-correct defaults from CSS vars at load. (B) `shared/emersus-renderer.js` now has a `remapLegacyWidgetColors` pass that runs on the widget body before srcDoc injection; it rewrites the documented legacy hex/rgba patterns in inline `style="..."` attribute values to their palette-aware vars, using a `prop\s*:\s*<value>` regex that skips JS string literals (so Chart.js config strings aren't mangled). Handles both future prompt-updated widgets and historical cached widget HTML in old threads. — `api/emersus/pipeline/prompt.js`, `api/emersus/pipeline/tools.js`, `shared/emersus-renderer.js`
- 2026-04-15 — Widget iframe palette now tracks the app theme. `shared/emersus-renderer.js` previously hard-coded the legacy site palette (#08080a base, #78dc14 green accent, dark-only) into every sandboxed LLM widget, so evidence cards and inline visuals stayed dark + lime even when the host was Paper·Royal. Rewrote `EMERSUS_THEME_CSS` to emit both Graphite·Jade and Paper·Royal token sets scoped on `:root[data-theme="mint|paper"]`, replaced every hardcoded rgba/#78dc14 rule with its semantic token (`--accent-primary`, `--accent-soft`, `--accent-line`, `--input-bg`, `--button-bg`, `--card-inset`, `--chart-axis/grid/border`), and added `--font-sans`/`--font-display` → Space Grotesk so widgets stop rendering Georgia-on-Mac. `WidgetFrame` reads `document.documentElement.dataset.theme`, subscribes to `emersus:themechange`, propagates the theme to the iframe's `<html data-theme>` attr, and re-renders srcDoc on palette flip. Chart.js defaults now read `--chart-axis/grid/border` via `getComputedStyle` so new `new Chart(...)` calls after a flip pick up the updated palette (existing chart instances stay in the creation-time palette — rebuilding a widget forces a refresh). Token names unchanged so legacy widgets keep working. — `shared/emersus-renderer.js`
- 2026-04-15 — Restored per-message references in chat_v2. Root cause: `chat-v2.css` hides `.chat-rail` via `display: none`, so `SourcesRailCard` — which its inline comment called "the single place sources appear" — became invisible when `chat_v2` flipped default-true. The topbar `N SOURCES CITED` pill is non-interactive and was the only remaining surface, so v2 users saw no references at all. Shipped a new compact per-message `SourcesFooter` React component in `shared/react-chat-app.js`: numbered chip strip + one-line rows (idx · ellipsized title · journal · year · tiny PubMed/DOI links · caret) with top 3 visible by default and the rest behind a "Show N more" button; click any row to expand its snippet + full `.cite-actions` row (PubMed / DOI / Ask follow-up). Clicking a chip for a hidden source auto-reveals the tail and scroll-jumps. `Expand all` + `Copy all` live in the header. `Ask follow-up` seeds the composer via the existing `buildFollowUpPrompt` helper — no auto-send. Off-v2 rail card left intact. CSS added under the existing Task-8 `/* CITATION FOOTER */` section of `shared/chat-v2.css`; old stubbed `.cite-actions` / `.cite-action` styles kept and merged. Mockups at `.superpowers/brainstorm/references-mock/option-1b-compact-footer.html` include a palette toggle for Graphite·Jade + Paper·Royal. — `shared/react-chat-app.js`, `shared/chat-v2.css`, `.superpowers/brainstorm/references-mock/*.html` (local-only)
- 2026-04-15 — Resurrected GitHub auto-deploy webhook. Had been silent for ~4 days (GitHub auto-disables after repeated failures). Root cause: pm2 cached the April-9 rotation secret in its in-memory env, and every subsequent `sed ~/app/.env + pm2 restart --update-env` failed to refresh it because `--update-env` re-reads the calling shell's environment, not the `.env` file. Processes that use `api/lib/load-env.js` (emersus-api/worker) were fine because they re-read the file on startup; `~/webhook.js` reads `process.env.WEBHOOK_SECRET` directly so it kept the stale value. Fix: rotated to a fresh 64-hex secret, synced GitHub webhook config, and stopped/deleted/restarted the pm2 webhook process with the secret explicitly exported in the start-up shell. Verified via self-signed HMAC → 200 OK → real deploy trigger. GitHub's "Recent Deliveries" now green. Gotcha saved to memory as `reference_pm2_env_gotcha.md` so future sessions don't re-hit it — `~/webhook.js` (on Hetzner, not in repo), `~/app/.env` (WEBHOOK_SECRET value rotated)
- 2026-04-13 — Rotated `OPENAI_API_KEY` on Hetzner prod (retroactively logged 2026-04-15 — the rotation happened without a changelog entry, so the task kept re-surfacing in checkpoints as "open"). No user-visible impact. Runbook for the next rotation saved in memory at `~/.claude/.../reference_openai_key_rotation.md`.
- 2026-04-15 — Post-redesign checklist sweep. Shipped ~12 follow-ups in one session. **Visual UX**: v2 shared sidebar on Train/Nutrition/Progress/Profile (matches chat's style — commit `b2470300`), labeled RPE chip group on workout set logger (`1aecde61`), water-hydration progress bar + inline supplement log form replacing `window.prompt()` (`d0cee956`), FOUC fix site-wide via pre-paint `data-theme` + v2 flag attribute resolution (`bbbbdc9d`), restyled legacy `/auth/callback/` page to v2 design system (`70981eef`). **Operator/infra**: fixed pm2 `webhook` deploy script on Hetzner (added missing `npm run build` step — root cause of the prior "looks the same" loop — plus per-request timestamped logging and `timingSafeEqual` length-mismatch guard); permissive `FOR SELECT` RLS policies added to `evidence_chunks` + `research_articles` (latent silent-empty risk for any future user-JWT path); `authenticator`/`authenticated` statement_timeout 8s→15s + `supabase-rest` restarted to cushion HNSW cold-cache scans (migration file at `supabase/20260415_corpus_rls_and_timeouts.sql`); `Cache-Control: no-cache` meta tags injected into 20 legacy redirect HTMLs (`scripts/inject-no-cache-legacy.js`); `scripts/mint-invite-token.js` gained `--email` flag that sends the invite link via Resend with branded HTML. Verified A.1 chat streaming + A.2 Google OAuth + A.3 workout session log → Progress page + A.4 water/supplement quick-log. A.5 profile-edit persistence verified at code level only. Checkpoint updated — `checkpoint.md`, `supabase/20260415_corpus_rls_and_timeouts.sql`, `scripts/{inject-no-cache-legacy,inject-app-sidebar,mint-invite-token}.js`, ~21 HTML files, `shared/{chrome,nutrition-v2,train-v2}.css`, `app/nutrition/nutrition-v2.js`, `shared/train/lift-active.js`, `~/webhook.js` (on Hetzner, not in repo)
- 2026-04-15 — Chat visual fix — ended the Frankenstein cascade fight + discovered the invisible-dist deploy bug. Commit `ef6ce094`: `app/index.html` now loads legacy `site.css`+`chat.css` only when `chat_v2` is OFF (conditional JS injection); `shared/chat-v2.css` stripped all 336 `!important` declarations (legacy sheets barely used them — 1 in chat.css, 2 in site.css — the cascade "fight" was phantom); removed three round-2 `display:none` hacks from commit `bc6f90bd` that were actively damaging (hiding `.thread-welcome-*` left the empty hero bare, hiding `.submit-orb` killed the send button, injecting `"+ ATTACH"` pseudo-content was placeholder noise for a nonexistent feature). **Root cause of four rounds of "looks the same"**: prior deploys were running `git pull && pm2 restart` without `npm run build`, so `dist/` was frozen at a pre-commit-12932396 state. Vite bundles `/shared/chat-v2.css` into `dist/assets/index-<hash>.css`; without `npm run build`, the bundle never updates and the CSS `<link>` references stale content. Verified post-fix: `https://emersus.ai/assets/index-Bp0OA5Jm.css` = HTTP 200 with the cleaned CSS. CLAUDE.md's "no build step" claim corrected (true only for local dev via `node server.js`; prod Caddy serves `dist/` which is a Vite MPA build output). Caveat: when `chat_v2` is OFF the boot script injects `/shared/site.css` which 404s in prod because Vite can only bundle statically-referenced paths; not blocking because default flag is true, but the legacy escape hatch needs Vite-visible imports if we ever need to turn the flag off on prod — `app/index.html`, `shared/chat-v2.css`, `CLAUDE.md`
- 2026-04-15 — Frontend redesign Phase 2 shipped (tag `redesign-phase-2-chat`, commits `f992d2c1..9540b3d0`): chat_v2 chrome with editable title + model pill + sources pill + share menu, AbortController-backed Stop button, always-visible message actions (Copy/Cite/Regenerate/Save plan/Swap meal/Export), thread share modal + `POST /api/threads/:id/share` + plain-text export stub + `/share/t/<token>` public render, citation footer (PUBMED/DOI/Ask follow-up) on sources rail, profile-aware empty-state prompt chips + `GET /api/emersus/suggest-prompts`, sidebar primary "+ New thread" button + 300ms-debounced search + Today/Yesterday/Previous-7/Earlier grouping, sidebar palette switcher, full chat-v2.css polish, default flipped to chat_v2=true. 76 new unit tests (407 total). Migration `20260415_threads_model_share.sql` still pending operator apply — required before share endpoints work in prod — `shared/feature-flags.js`, `shared/chat/{top-bar,message-actions,share-modal,empty-prompts,sidebar-helpers,widget-footers}.js`, `shared/chat-v2.css`, `shared/react-chat-app.js`, `api/emersus/{threads-share,suggest-prompts}.js`, `server.js`
- 2026-04-09 — Added CLAUDE.md memory system (overview/scripts/schema docs + checkpoint + changelog) — `CLAUDE.md`, `docs/overview.md`, `docs/scripts.md`, `docs/schema.md`, `checkpoint.md`, `changelog.md`
- 2026-04-09 — Bot scope & refusal overhaul: positive-framing system prompt, classifySafety collapsed to binary {allowed, hard_refusal} with intent-based PED + tightened medication regex, conversational per-category refusal messages, guardrail_events CHECK constraint loosened — `api/emersus/workflow.js`, `supabase/20260409_guardrail_events_hard_refusal.sql`, `docs/superpowers/specs/2026-04-09-bot-scope-overhaul-design.md`
- 2026-04-09 — Deployed bot scope overhaul to production Hetzner box (commit `b71505cf`). First-ever application of `supabase/20260405_guardrail_events.sql` (the original migration had never run on prod — refusal telemetry had been silently failing). Discovered other missing migrations (`chat_token_usage_events`, `chat_token_usage_cached`, `email_provider_allowlist`) and the pre-existing `retrieveDatabaseEvidence.js:31` error — both deferred to a separate audit session — Hetzner deploy via PM2 + GitHub webhook auto-deploy
- 2026-04-09 — Rotated GitHub deploy webhook HMAC secret on Hetzner box after the original was leaked into a chat transcript during the bot scope deploy. Generated new secret on the box (never displayed), updated `/home/emersus/webhook.js` line 5 via on-box sed with backup + node syntax check, restarted pm2 webhook process, pushed new secret to GitHub webhook config. Note: had to rotate twice — the first replacement also leaked when its `cat` output ended up in pasted scrollback. Final rotation used a separate isolated Termius session for the cat → GitHub paste, with the working session never seeing the value — `/home/emersus/webhook.js` (on box, not in repo)
- 2026-04-10 — Guardrail hardening: expanded prompt injection detection from 7 to ~60 regex patterns (DAN/STAN/AIM, authority spoofing, encoding evasion, multi-language), added thread drift detection (Layer C) for short-message off-topic drift, escalating cooldown (3/5/8 blocks → 30s/2min/5min), bot/script detection scoring (interval consistency, duplicate payloads, suspicious UA, block ratio), raised rate limit 10→15/5min (bots get 3), extracted shared `rate-limit.js` module — `api/emersus/workflow.js`, `api/emersus/rate-limit.js`, `api/emersus/recommendation.js`, `api/emersus/recommendation-stream.js`, `supabase/20260410_guardrail_events_bot_cooldown.sql`, `scripts/test-guardrail-classifier.js`
- 2026-04-10 — Workout tracking: exercises catalog (59 seeded), workout_logs table with exercise matching pipeline (exact/alias/fuzzy/auto-create), 8 analytics RPCs, progress dashboard with stats/charts/muscle volume/PRs, exercise detail drill-down with progression chart, session detail drill-down, planner stats strip integration, backfill script. Security: auth guard on upsert RPC, SECURITY INVOKER on all read RPCs — `supabase/20260411_*.sql`, `app/progress/**`, `shared/exercise-icons.js`, `shared/progress-charts.js`, `shared/progress-helpers.js`, `shared/supabase.js`, `app/workout/session/session.js`, `app/workout/workout.js`, `scripts/backfill-workout-logs.js`
- 2026-04-10 — Weight unit preference (kg/lbs): nullable `profiles.weight_unit` column with locale-based default (en-US → lbs), shared `unit-conversion.js` helpers (`resolveWeightUnit`, `formatWeight`, `formatVolumeWithUnit`, `parseLoadString`, `displayLoadString`), session view stores canonical kg in `completed_blocks` and converts at input/display edges, progress pages + planner stats strip render in user's unit, editable profile toggle, LLM system prompt passes `user_profile.weight_unit` and onboarding asks for preference — `supabase/20260411_profile_weight_unit.sql`, `shared/unit-conversion.js`, `shared/progress-charts.js`, `shared/app-pages.js`, `app/profile/index.html`, `app/workout/session/session.js`, `app/workout/workout.js`, `app/progress/**`, `api/emersus/workflow.js`
- 2026-04-10 — Share cards + outdoor tracking (Phase 2): 3 new session views (`app/workout/cardio/`, `swim/`, `climb/`) for activities that don't fit the resistance model — cardio with GPS (`watchPosition` + jitter filter + Haversine + pause markers + Screen Wake Lock), pool swim with lap-tap counter and splits, climbing with per-route logging (V/YDS/Font/French grade systems, flash/send/project). Canvas 2D share card renderer (`shared/share-card.js`) with 6 variants (gym, cardio_map, cardio_time, swim, climb, hybrid), reusable `shared/share-modal.js` with Web Share API + Download + Copy fallbacks, Mapbox Static API integration with polyline encoding + privacy crop (configurable 0-500m radius on `profiles.mapbox_privacy_radius_m`). `upsert_workout_logs` v2 branches by `block_category` (client-provided, authoritative) — critical fix from review: was previously inferring from field shape which mis-routed swim/climb to cardio branch. Planner routes to the correct session view by first block's exercise category with regex fallback (`inferCategoryFromName`). Profile settings panel gained sharing & tracking fields (display_name_public, path privacy radius, default pool length, grade system, distance_unit). LLM system prompt teaches per-category block shapes and onboarding asks `preferred_sports` with pool length / grade system follow-ups. Mapbox public token exposed via `/api/config` (`MAPBOX_PUBLIC_TOKEN` env var, URL-restricted to emersus.ai/*) — token must be set manually in prod `.env.local`; absent token → cardio variant falls back to time-only layout — `supabase/20260412_*.sql`, `shared/{mapbox,gps-tracker,climbing-grades,share-card,share-modal}.js`, `shared/unit-conversion.js` (distance helpers), `app/workout/{cardio,swim,climb}/`, `app/workout/workout.js`, `app/workout/session/session.js`, `app/profile/index.html`, `shared/app-pages.js`, `api/config.js`, `api/emersus/workflow.js`, `.env.example`
- 2026-04-09 — Migration audit + retrieveDatabaseEvidence fix (Followup 2). Fixed pgvector schema-qualification bug: `match_evidence_chunks` had no `SET search_path`, so the `<=>` operator (in `extensions` schema) was invisible to PostgREST's default `public`-only search path. Fix: `ALTER FUNCTION ... SET search_path = public, extensions`. Applied three missing migrations: `chat_token_usage_events` (table + indexes + RLS), `chat_token_usage_cached` (added `cached_prompt_tokens` column), `email_provider_allowlist` (idempotent re-run, table existed, trigger recreated). Added GRANTs for PostgREST schema discovery + restarted `supabase-rest`. Result: `evidenceCount: 6` (was 0), zero PGRST205 errors (was every request), clean error log — `supabase/20260409_match_evidence_chunks_search_path.sql`, `supabase/20260406_chat_token_usage_events.sql`, `supabase/20260408_chat_token_usage_cached.sql`, `supabase/20260408_email_provider_allowlist.sql`
- 2026-04-10 — Fix gym share card showing 0kg after a logged session. Three bugs in `computeGymSummary`/`finishSession`: (1) `if (!set.done) continue` silently dropped every set the user hadn't tapped the "Done" button on — but `upsertWorkoutLogs` stores all populated rows regardless, so the share card disagreed with the source of truth; removed the filter. (2) `finishSession` built `nextPlan` off the stale React `plan` closure and its initial `flushSave()` was non-awaited, so users who tapped "Finish & share" inside the 800ms debounce window computed the summary on pre-save data; now `await`s the pending flush and reads `planRef.current.plan`. (3) `computeGymSummary` only looked up blocks in `session.blocks`, dropping warmup volume; now checks `warmup_blocks` too, matching `upsertWorkoutLogs` — `app/workout/session/session.js`
- 2026-04-10 — Harden workout session inputs end-to-end. Client: fixed the load input placeholder showing "RPE 8" / "bodyweight" for RPE-based prescriptions (now parses via `parseLoadString` and shows neutral "load" when non-numeric). Added `maxLength` caps on every input (reps 15, load 8, rpe 4, per-set notes 300, session notes 500) enforced at HTML-attribute, updateSet-keystroke, and serializeBlockEntry save layers. Clamped RPE to Borg 0–10 scale on every keystroke (rounds to one decimal, allows mid-entry like `8.`). Added a client-side `sanitizeNotes` that strips 15 prompt-injection pattern families from set/session notes before they hit the plan JSONB (model-adjacent data since workflow.js JSON.stringifies `current_workout_plan` straight into the user message). `createEmptyActualSet` no longer prefills `reps` with the LLM prescription string (was silently storing "8-12" / "20-40 sec" as reps, which crashed `workout_logs.reps::smallint` and made no semantic sense). DB: new migration `20260413_upsert_workout_logs_v3.sql` drops the `set.done` filter in the resistance branch (mirror of the client-side fix), defensively extracts leading digits from reps/load/rpe via `substring(... FROM '^[0-9]+...')` so legacy rows with prefill junk don't abort the INSERT, clamps RPE to [0, 10] server-side. Applied to Hetzner via supabase_admin; `scripts/backfill-upsert-workout-logs-v3.js` replayed 2 existing sessions into `workout_logs` (18 rows) so the Progress page reflects reality — `app/workout/session/session.js`, `shared/workout-plan-schema.js`, `supabase/20260413_upsert_workout_logs_v3.sql`, `scripts/backfill-upsert-workout-logs-v3.js`
- 2026-04-11 — Fix workout-plan fence truncation regression. Two fixes in `api/emersus/workflow.js`: (1) Bumped `max_output_tokens` from 8000 → 16000 on `callOpenAISynthesis`. The 8000 ceiling was sized before `warmup_blocks` became the default for every non-deload session; each now adds ~150 tokens, pushing an 8-week 4-day plan past 10k tokens and truncating mid-JSON. Verified against the real failing response (`resp_09fea4231f7d919f...`, status=incomplete, reason=max_output_tokens, output_tokens=8000 exactly). Replayed the same input with 16000 → completed at 12421 tokens, 48 sessions parsed cleanly. (2) `splitSynthesisIntoSegments` only matched CLOSED fences, so a truncated `\`\`\`workout-plan` fence fell into a plain text segment, `stripStrayFenceMarkers` erased the opener, and the frontend saw naked JSON — the client-side "Plan was cut off" fallback (added 05fecc06) never fired because the server mangled the text first. Now detects trailing unclosed workout-plan fences and promotes them to `workout-plan` segments with `_unclosed: true`; reassembly emits them without a closer so the client's truncation detector fires. Verified end-to-end against the real failing response: `hasWidgetFences` now true, parser produces `workout-plan` segment with `truncated: true` — `api/emersus/workflow.js`
- 2026-04-11 — Close server-side prompt-injection holes in workflow.js. (1) `PROFILE_INJECTION_PATTERNS` regex hardening: `/ignore (all |previous |prior |above )?instructions/` used a single `?` slot that only caught one qualifier word — "ignore all previous instructions" (two words, the canonical DAN phrase) slipped through. Rewrote the verb→noun patterns as `<verb>\s+(?:\w+\s+){0,3}<noun>` so 0–3 intermediate qualifier words match, added `/disregard/` sibling. Now catches "bypass all safety filters", "override all safety instructions", etc. 13 phrase-level tests pass; "rules of thumb" and "safety first" still pass through because they lack the verb prefix. (2) Added `sanitizeWorkoutPlanForModel` that walks every user-writable free-text field in `currentWorkoutPlan` (title, notes, session summary/notes, blocks[].notes, warmup_blocks[].notes, completed_blocks[].session_notes, actual_sets[].notes) and strips injection patterns BEFORE the plan gets JSON.stringified into the LLM user message. Wired in at the construction site so it runs on every synthesis call. This is the server-side read complement to the client-side write-time `sanitizeNotes` from the session.js hardening — neither layer alone suffices because an attacker can bypass the client by hitting the REST/RPC endpoint directly — `api/emersus/workflow.js`
- 2026-04-11 — PubMed topic list expansion + validator. Brainstormed → spec → plan → implementation cycle that grew `TOPIC_QUERIES` from the hand-curated 121-topic list to 302 topics organized into 21 commented domain sections (Core resistance, Exercise selection, Endurance, Body comp/nutrition, Supplements-performance, Supplements-hormones, Supplements-peptides, Recovery, Metabolic-cardio, Mind/behavior, Women's health 16, Youth 16, Masters 16, Injury rehab 20, Endurance spec 18, Advanced programming 16, Sport-specific 20, Mental/behavioral 14, Nutrition subfields 14, Metabolic/longevity 15, Mobility 14). Added `scripts/validate-pubmed-queries.js` — rate-limited PubMed esearch smoke test (~3 RPS) reporting PASS (≥100) / WARN (≥10) / FAIL (<10) per query. Also hardened `fill-pmc-topics.js` parent-process dotenv loading + per-topic try/catch so transient NCBI errors don't kill the whole 300-topic run. Ran the fill on Hetzner via nohup + disown overnight; corpus grew from ~209k → ~295k before Phase 2 deploy superseded the hand-script approach — `scripts/fill-pmc-topics.js`, `scripts/validate-pubmed-queries.js`, `docs/superpowers/specs/2026-04-11-pubmed-topic-expansion-design.md`, `docs/superpowers/plans/2026-04-11-pubmed-topic-expansion.md`
- 2026-04-11 — Email routing + signup alerts. Switched every user-facing email address from `support@emersus.ai` → `info@emersus.ai` (contact page, terms, privacy, CONTACT_EMAIL constant in shared/supabase.js). On Hetzner, set RESEND_FROM_EMAIL="Emersus AI <info@emersus.ai>", CONTACT_NOTIFICATION_EMAIL, WAITLIST_NOTIFICATION_EMAIL, SIGNUP_NOTIFICATION_EMAIL, ADMIN_EMAILS, ALERT_EMAILS all pointing at info@. Added `api/notify-signup.js` — server-validated admin alert endpoint that looks up the new user in Supabase auth (service-role) with a 5-min recency guard to block spoofed pings, then sends a branded Resend email to the operator. Hooked client-side `shared/auth-pages.js` in both the email+password signup flow and the `handleCallbackPage()` OAuth return path (`maybeNotifyFreshOAuthUser` — same 5-min check). Verified: operator personal Gmail and `info@emersus.ai` both received test sends from `info@emersus.ai` as the FROM. DNS state: MX → smtp.google.com (receives), SPF missing Resend include but DKIM alignment via `resend._domainkey.emersus.ai` carries DMARC (`p=quarantine`) — `api/notify-signup.js`, `shared/auth-pages.js`, `shared/supabase.js`, `server.js`, `contact/index.html`, `terms/index.html`, `privacy/index.html`, `~/app/.env` on Hetzner
- 2026-04-11 — Topic discovery pipeline + multi-source ingestion — phase 2 deploy. 76-commit feature branch merged to main and deployed to Hetzner in one session. **Schema**: renamed `pubmed_articles` → `research_articles` (295,944 rows preserved), added `source`/`peer_reviewed`/`external_id`/`source_metadata` columns, created `research_topics` (302 seeded), `topic_candidates`, `discovery_feeds` (21 seeded), `job_progress`, `worker_heartbeats`, `alert_log` tables. `match_evidence_chunks` v2 deployed with retraction filter preserved + perf hotfix reverting to v1's single-CTE shape after the window-function version triggered a 30s planner pathology via RPC (18ms as direct SQL — see hotfix comment for the explanation). **New infra**: `emersus-worker` pm2 process running `worker/index.js` with 13 pg-boss handlers + 4 cron schedules (`discovery-weekly` Mon 03:00 ET, `detect-failure-clusters` every 5 min, `alert-daily-digest` daily 08:00 ET, `cleanup-job-progress` daily 02:00 ET). Heartbeat-watchdog crontab entry (every 2 min, system cron not pg-boss) fires Resend alert to ALERT_EMAILS if worker heartbeat > 5 min stale. Admin UI live at `/admin/{index,candidates,topics,feeds,jobs,alerts}` with env-allowlist auth. **Bugs caught in prod during the deploy** (all fixed + committed): (1) match_evidence_chunks v2 perf pathology — hotfix `20260412_match_evidence_chunks_v2_perf_hotfix.sql`; (2) `boss.work()` in pg-boss v10 does NOT create the pgboss.queue row — `jobs/_registry.js` now calls `createQueue(name)` inside the register helper (otherwise child sends were silently dropped); (3) handler schema drift — `ingest-topic-from-source.js` used `published_at` / text[] authors against the real `publication_date`/jsonb authors schema; (4) non-pubmed rows can't be inserted because `research_articles.pmid` is NOT NULL PK — phase 2 restricted to `SUPPORTED_SOURCE_IDS = ['pubmed']`, multi-source rework deferred as follow-up. **Fill restarted**: all 302 topics enqueued via `scripts/fill-pmc-topics.js --all`; existing topics dedup fast via ON CONFLICT, unfilled topics ingest at ~3 min each. Pre-deploy backup taken: `~/backups/db_20260411_180424_pre-phase1.sql.gz` (8.5 GB compressed, verified complete via pg_dump trailer). Phase 3 (multi-source schema rework) deferred; `evidence_chunks` still joins on pmid so non-pubmed rows aren't retrievable yet — `supabase/20260412_*.sql`, `worker/*.js`, `jobs/*.js`, `scripts/sources/*.js`, `api/admin/*.js`, `api/lib/alerts.js`, `scripts/{lib/run-as-job,heartbeat-watchdog,fill-pmc-topics,seed-research-topics,seed-discovery-feeds}.js`, `admin/{index,candidates,topics,feeds,jobs,alerts}/index.html`, `docs/superpowers/specs/2026-04-11-topic-discovery-pipeline-design.md`, `docs/superpowers/plans/2026-04-11-topic-discovery-pipeline.md`, `docs/ops/topic-pipeline-runbook.md`
- 2026-04-11 — Installed ripgrep 14.1.0, fd-find 9.0.0, plocate 1.1.19 on Hetzner via `apt install ripgrep fd-find plocate`. plocate registered systemd timer `plocate-updatedb.timer` for daily index rebuilds — no manual cron. Benchmark: recursive content search over `~/app/` drops from ~10-30s (grep -r) to 21ms (rg). Binary is `fdfind` (not `fd`) due to Debian naming conflict. Future ssh commands should use `rg` / `fdfind` instead of `grep -r` / `find` — `~/app/.bashrc` (note: alias not added, use `fdfind` directly)
- 2026-04-12 — Full fix on the Task 18 follow-up bugs (commit `15fa25b3`). Three bugs fixed in one commit after the post-Task-18 fanout made the failure modes concrete: (1) `jobs/_registry.js` was passing `{ teamSize: 4, teamConcurrency: 4 }` to `boss.work()`, but those are pg-boss **v9** options — in v10 the only valid fetch option is `batchSize` (default 1), so the v9 names were silently ignored and every queue ran a single-worker fetch loop. That's why a slow `ingest-topic-from-source` job blocked every sibling in the same fanout during Task 18. Fix: new `concurrency` option in the `register()` helper that calls `boss.work()` N times to create N independent workers for the same queue, each with its own fetch cycle. ingest-topic-from-source now has 4 parallel workers. (2) The same helper had a latent silent-data-loss hazard: `const jobRow = Array.isArray(job) ? job[0] : job;` — in v10 the work callback ALWAYS receives an array of job rows, so this dropped every job after the first if `batchSize > 1`. Fixed by iterating the batch via `Promise.all()` and returning per-job results. Not currently reachable (effective batchSize was 1) but pinned via regression tests so it won't silently regress. (3) biorxiv/medrxiv/sportrxiv client-side scans could walk all 12 monthly chunks (biorxiv/medrxiv) or the entire OSF preprint pool (sportrxiv) even when the topic yielded zero matches, which is pointless and slow. Added newest→oldest scan direction, `MAX_CONSECUTIVE_EMPTY_CHUNKS=3` early-exit on biorxiv/medrxiv, `MAX_PAGES_PER_CHUNK=5` per-chunk safety cap, and `MAX_PAGES=10` hard cap on sportrxiv's flat pagination. Added `tests/unit/jobs/_registry.test.js` (6 new tests pinning the concurrency contract + batch-iteration behavior) and a biorxiv early-termination regression test. **Validation**: re-ran the Task 18 fanout against topic 1 with target=20. `core` completed in 6 seconds (15 inserted + 5 skipped) WHILE `biorxiv` was still running its scan — the smoking-gun proof that the concurrency fix works. Previously core would have been stuck behind biorxiv. Cross-source corpus grew +95 rows this session. 210 → 217 tests passing — `jobs/_registry.js`, `scripts/sources/{biorxiv,medrxiv,sportrxiv}.js`, `tests/unit/jobs/_registry.test.js`, `tests/unit/sources/biorxiv.test.js`
- 2026-04-12 — Post-Task-18 hardening + second fanout validation (commit `4bfec11c`). Two bugs caught by a live `ingest-topic` fanout to the creatine topic (target=20, all 9 sources minus epistemonikos): (1) `ON CONFLICT (pmid) DO NOTHING` in `jobs/ingest-topic-from-source.js` only caught pmid PK collisions, so a re-ingestion whose external_id matched a previous row triggered a 23505 on the `(source, external_id)` UNIQUE index and aborted the handler, burning the pg-boss retry budget. Fixed by changing to targetless `ON CONFLICT DO NOTHING` which catches violations on any unique constraint. Added regression test that greps the SQL template for the specific form. (2) biorxiv/medrxiv/sportrxiv client-side topical filter used `.some()` over a flat term list, so a paper mentioning "drug resistance" passed a creatine-resistance-training query. Replaced with new shared `scripts/sources/_query-match.js` helper (exports `parseQueryIntoGroups` + `matchesQueryGroups`) that splits on top-level AND, preserves quoted phrases as multi-word substring terms, and requires at least one term from EVERY AND-group to appear in title+abstract. Added 10 unit tests. **Second fanout outcome**: 6 of 9 sources completed successfully (europepmc/openalex/semantic-scholar/openaire each with 15 new inserts + 5 correctly-skipped dupes from Task 17). +80 new cross-source creatine papers in `research_articles`. Also noticed pg-boss teamSize=4 batching holds up the fanout when one source is slow — biorxiv's 3-min 365-day scan blocked the next batch from starting. Cancelled biorxiv/core mid-run; logged as open followup in `checkpoint.md`. Test suite 199 → 210 (+11) — `jobs/ingest-topic-from-source.js`, `scripts/sources/_query-match.js`, `scripts/sources/{biorxiv,medrxiv,sportrxiv}.js`, `tests/unit/sources/query-match.test.js`, `tests/unit/jobs/ingest-topic-from-source.test.js`
- 2026-04-12 — Tool-routed workflow redesign (4 stages). Replaced regex intent classification with OpenAI Responses API tool calls (`emit_meal_plan`, `emit_workout_plan`). Model self-routes via tool selection; server validates + wraps in fences for client compatibility. Guardrail reduced from 300-term scope gate to 3 targeted safety matchers (PED, self-harm, injection). Normalization pipeline simplified: removed `autoWrapBareWorkoutPlan`, `autoWrapBareHtml`, unclosed fence detection. ~565 lines removed, workflow.js went from 4890 to 4325 lines — `api/emersus/workflow.js`, `shared/widget-fence-parser.js`, spec: `docs/superpowers/specs/2026-04-12-tool-routed-workflow-design.md`, plan: `docs/superpowers/plans/2026-04-12-tool-routed-workflow.md`
- 2026-04-12 — Multi-source ingestion enablement (phase 3). Lifted the phase 2 pubmed-only restriction via a synthetic pmid sequence (`research_articles_synthetic_pmid_seq` starting at 10^10, applied to Hetzner via supabase_admin). **5 new adapters**: OpenAlex (polite-pool, inverted-index abstract reconstruction), Semantic Scholar (x-api-key, boolean-query sanitizer), Epistemonikos (Apikey header, lazy-gated on `EPISTEMONIKOS_API_KEY`), OpenAIRE (rewritten against their Graph v1 API at /graph/v1/researchProducts after the legacy /search/publications endpoint was found unresponsive; keyword cap to 3 because Graph AND-matches terms), CORE (Bearer token, trailing-slash URL to avoid redirect, `journals[0].title` for journal name, strip wrapping quotes from publisher strings). **Ingest handler changes**: `jobs/ingest-topic-from-source.js` allocates synthetic pmids from the sequence when the source is non-pubmed; `jobs/ingest-topic.js` gained `MULTI_SOURCE_ENABLED` env flag + `INGEST_DISABLED_SOURCES` per-source disable list + `DEPRIORITIZED_SOURCE_IDS=['crossref','doaj']` filter. **Retrieval**: `dedupByDoi` helper in `api/emersus/retrieveDatabaseEvidence.js` collapses cross-source duplicates (same DOI indexed by pubmed + openalex as separate rows) down to the highest-similarity chunk; added `source`/`external_id` to the SELECT + the row shape. **Citation rendering**: new isomorphic `shared/citation-format.js` with `formatCitationUrl` + `formatCitationLabel` + `SYNTHETIC_PMID_FLOOR` used by `api/emersus/workflow.js` (5 call sites), `shared/react-chat-app.js` (1 site), and inlined into `chat/index.html` legacy text/plain script block. **Prod surprises caught in Task 17 smoke tests** (all fixed + re-deployed): (a) `research_articles_source_check` CHECK constraint only allowed the original 7 sources, blocking all 5 new adapters — expanded via `20260412_research_articles_source_check_expand.sql`; (b) OpenAIRE legacy endpoint dead (40s timeout) — rewrote to Graph v1; (c) Semantic Scholar + OpenAIRE don't parse boolean queries — added `sanitizeToKeywords` helper; (d) OpenAIRE Graph AND-matches keywords so full query collapses `numFound` to 1 — capped to first 3 unique keywords. **Prod rollout**: applied 2 migrations to Hetzner (`supabase_admin -p 5433`), pushed 15 commits, set `MULTI_SOURCE_ENABLED=true`, `OPENALEX_POLITE_EMAIL=info@emersus.ai`, `CORE_API_KEY=<set>`, `INGEST_DISABLED_SOURCES=epistemonikos` (blocked on their email reply), `pm2 restart emersus-worker --update-env`. **Smoke test outcomes**: all 8 active sources inserted 5 papers each for the creatine topic (openalex, core, europepmc, biorxiv, medrxiv, sportrxiv, semantic-scholar, openaire — 41 rows total, synthetic pmids 10000000001–10000000041). OpenAlex/OpenAIRE/S2/CORE/europepmc returned topic-relevant creatine papers; biorxiv/medrxiv/sportrxiv returned recent preprints regardless of query (pre-existing adapter limitation — their REST APIs don't have keyword search; `fetchPapers` falls through to the discovery-style latest-preprints path). Test suite 162 → 199 passing (+37 tests: 15 adapter tests, 10 citation-format, 5 dedup, 4 multi-source flag, 3 openaire, synthetic pmid, etc.) — `supabase/20260412_research_articles_synthetic_pmid_sequence.sql`, `supabase/20260412_research_articles_source_check_expand.sql`, `jobs/ingest-topic.js`, `jobs/ingest-topic-from-source.js`, `jobs/_registry.js`, `scripts/sources/{openalex,semantic-scholar,epistemonikos,openaire,core}.js`, `shared/citation-format.js`, `api/emersus/retrieveDatabaseEvidence.js`, `api/emersus/workflow.js`, `shared/react-chat-app.js`, `chat/index.html`, `.env.example`, `tests/**`, plan: `docs/superpowers/plans/2026-04-11-multi-source-enablement-plan.md`, spec: `docs/superpowers/specs/2026-04-11-multi-source-enablement-design.md`
- 2026-04-11 — Phase 2 hotfix: NCBI rate-limit + pipeline hardening. Diagnosed 14 permanently-failed `ingest-topic-from-source` jobs from the earlier deploy (all ECONNRESET / socket-hangup during a 19:20-19:46 UTC window, not rate-limit 429s as first glance suggested). Root cause was three compounding issues: (1) `NCBI_API_KEY` was set in `~/app/.env` but never wired into `scripts/sources/pubmed.js` URL builders — the comment even said "3 RPS unauthenticated" and no one had ever added `appendAuth()`; (2) `scripts/sources/_ratelimit.js` had a concurrency race where N awaiting callers all read the same stale `nextSlotAt`, all slept the same duration, all woke in the same event-loop tick, and returned their "go ahead" sequentially within one ms — effect was bursts of ~14 requests every ~4.6s instead of evenly-spread 3 RPS, which NCBI responds to by TCP-dropping rather than 429ing; (3) pg-boss `retryLimit` was the default 2 with no backoff, insufficient to absorb NCBI's drop-under-load pattern (evidence: 52 of 302 completed jobs succeeded on retry, ~17% transient failure rate). **Fixes**: pubmed.js now reads NCBI_API_KEY lazily per request and appends `api_key`/`tool=emersus`/`email=info@emersus.ai`, limiter bumped to 9 RPS when key present (margin under NCBI's 10); _ratelimit.js reserves slots synchronously before any await (critical section pre-await, race-free under JS single-threaded guarantees); `jobs/ingest-topic.js` sends with `retryLimit: 5, retryBackoff: true, retryDelay: 15`; `jobs/_registry.js` drops `ingest-topic-from-source` teamSize 14→4 for limiter headroom; `jobs/embed-batch.js` honors explicit `null` vs `undefined` openaiClient (was using `??` which collapsed both — hid a pre-existing test failure). Also fixed 2 pre-existing broken tests that had drifted from the phase 2 schema cut: `tests/unit/jobs/ingest-topic.test.js` expected 2 source fanout when the phase-2 filter only keeps pubmed, and `tests/unit/jobs/ingest-topic-from-source.test.js` used fake papers with non-numeric `externalId` that didn't pass the pmid filter. Added 5 regression tests (limiter concurrency spread, pubmed URL with/without key, retry options assertion). Deployed as commit `dcf3d877`. **Requeue**: bypassed the 24-hour `singletonKey` dedup on the old failed rows by sending directly to `ingest-topic-from-source` with a fresh `requeue-2026-04-11-{id}-pubmed` singleton suffix. All 14 topics (`volume`, `frequency`, `exercise_selection`, `barbell_training`, `fitness_programs`, `adherence`, `pcos_and_exercise`, `pelvic_floor_training`, `female_hypertrophy_protocols`, `female_sex_hormones_performance`, `reaction_time_aging`, `acl_rehab`, `low_back_rehab`, `achilles_tendinopathy`) completed one-shot, 0 retries each, avg ~21s per topic. Corpus: 411,243 → 414,886 rows (+3,643). Test suite: 153/157 → 162/162. Also installed automated nightly DB backup: `~/scripts/backup-db.sh` uses `docker exec supabase-db pg_dump | pigz` (parallel gzip, 3:47 wall clock for 8.1 GB vs projected 175 min with initial `gzip -9`), 3-day retention with `*_daily.sql.gz` glob that leaves hand-labeled snapshots untouched, < 20 GB disk-free warning. Systemd timer `emersus-db-backup.timer` installed in `/etc/systemd/system/`, enabled + running, first fire 2026-04-12 05:00 UTC. — `scripts/sources/pubmed.js`, `scripts/sources/_ratelimit.js`, `jobs/ingest-topic.js`, `jobs/_registry.js`, `jobs/embed-batch.js`, `tests/unit/sources/{pubmed,ratelimit}.test.js`, `tests/unit/jobs/{ingest-topic,ingest-topic-from-source}.test.js`, `/home/emersus/scripts/backup-db.sh`, `/etc/systemd/system/emersus-db-backup.{service,timer}`
- 2026-04-13 — Nutrition subsystem: meal planning, journaling, supplements (Phases 1–6) — `supabase/20260414_*.sql`, `api/emersus/meal-plans.js`, `api/emersus/meal-journal.js`, `api/emersus/nutrition-parser.js`, `api/emersus/foods-search.js`, `api/emersus/rpc-proxy.js`, `shared/meal-plan-*.js`, `shared/nutrition-*.js`, `shared/food-detail-drawer.js`, `app/nutrition/`, `app/progress/nutrition-pane.js` + `progress.js` tab switcher, `scripts/import-usda-foods.js`
- 2026-04-13 — Workflow pipeline redesign: decomposed the 4325-line workflow.js monolith into 11 pipeline modules (~1830 lines total). All structured output now flows through OpenAI Responses API tool calls with `strict: true` (emit_meal_plan, emit_workout_plan, emit_widget, log_food). SSE streaming delivers prose in real-time and tool results as complete validated objects. Fence-based output format eliminated entirely. System prompt reduced from ~2400 to ~120 lines. 54 new unit tests. 15 commits on `feat/pipeline-workflow`, merged to main and deployed to Hetzner — `api/emersus/pipeline/*.js` (11 modules), `api/emersus/workflow.js` (new orchestrator), `api/emersus/recommendation.js`, `shared/react-chat-app.js`, `shared/emersus-renderer.js`, spec: `docs/superpowers/specs/2026-04-13-workflow-pipeline-redesign.md`, plan: `docs/superpowers/plans/2026-04-13-workflow-pipeline-redesign.md`
- 2026-04-12 — Quadrupled exercise seed catalog from 69 → 280 exercises (+211 new). Added Olympic lifts, kettlebell, calisthenics, plyometrics, strongman, bands, TRX/suspension, cable, machine, trap bar, landmine, core/abs, cardio drills, sport-specific drills, swimming drills, climbing, flexibility/stretching. New equipment values: kettlebell, band, suspension, rings, sled, landmine, trap_bar. — `supabase/20260412_exercises_quadruple_seed.sql`
- 2026-04-12 — Fix UTC date bug: `new Date().toISOString().slice(0,10)` returns UTC date, not local — users west of UTC had food logs, workout sessions, streak calculations, and day-type resolution land on the wrong calendar day (e.g. 7pm ET = next day in UTC). Replaced all 12 client-side call sites with `localDateStr()`/`localDateOffset()` from new `shared/date-utils.js` which use `getFullYear/getMonth/getDate` (browser-local). Also patched `NutritionLogConfirmCard` to override the server-provided `logged_date` with the client's local date at parse time — `shared/date-utils.js`, `shared/{react-chat-app,nutrition-today-panel,nutrition-supplements-panel,nutrition-journal-panel,food-detail-drawer,workout-plan-selectors,supabase,progress-helpers}.js`, `app/progress/nutrition-pane.js`
- 2026-04-12 — Security hardening pass across API + infra. Express: explicit 100kb body limits, security response headers (X-Content-Type-Options, X-Frame-Options, Referrer-Policy, Permissions-Policy), disable x-powered-by, 30s request timeout (SSE exempt), specific HTTP method routing (app.post not app.all), catch-all 404 for /api/*. Rate limiting: added per-IP rate limiter for public POST endpoints (contact/waitlist/check-email/notify-signup, 10-20 req/15min), stale entry cleanup every 10min. Email XSS: HTML-entity-escape all user-supplied values in contact/waitlist/notify-signup email templates. Widget iframe: added CSP meta tag (default-src 'none' + allowlist), tightened postMessage target to location.origin (was wildcard), capped iframe height to 5000px, origin-check on incoming messages, truncate sendPrompt to 2000 chars. Input limits: explicit .slice() caps on all public endpoint string fields. Caddy: security header snippet (X-Content-Type-Options, X-Frame-Options, Referrer-Policy, Permissions-Policy, CSP), blocked /infra/* /.git/* /.claude/*, admin path filter on supabase.emersus.ai. Docker: no-new-privileges on all 6 services, read-only rootfs on kong/auth/rest/meta with tmpfs, removed unused 8443 Kong port — `server.js`, `api/emersus/rate-limit.js`, `api/contact.js`, `api/waitlist.js`, `api/notify-signup.js`, `shared/emersus-renderer.js`, `infra/Caddyfile`, `infra/docker-compose.yml`
- 2026-04-13 — Waitlist verification hardening (commits `c5bf6c60`, `47b18022`, `9a2fa83c`, committed on another device, pulled 2026-04-14). Added Cloudflare Turnstile challenge to the waitlist form (`api/lib/turnstile.js` siteverify wrapper + env-gated via `TURNSTILE_SITE_KEY`/`TURNSTILE_SECRET_KEY`) and an email-verification round-trip before a signup is persisted: `api/waitlist.js` now issues an HMAC-signed self-contained token (`api/lib/waitlist-verification.js`, base64url payload + sha256 sig, 3-day TTL, keyed off `WAITLIST_VERIFICATION_SECRET`) and emails a confirm link; `api/waitlist-confirm.js` verifies the token server-side and only then writes the waitlist row. Follow-up fix `47b18022` restored the Turnstile widget styling (`styles.css`); `9a2fa83c` added a frontend rebuild step to `scripts/deploy-app.sh` so bundled Vite output ships with deploys. New unit test `tests/unit/api/waitlist-verification.test.js`. `.env.example` + `package.json` updated. — `api/waitlist.js`, `api/waitlist-confirm.js`, `api/lib/turnstile.js`, `api/lib/waitlist-verification.js`, `api/config.js`, `server.js`, `script.js`, `scripts/deploy-app.sh`, `styles.css`, `.env.example`, `package.json`, `tests/unit/api/waitlist-verification.test.js`
- 2026-04-13 — Resend hosted templates support (commit `d89c41a2`, committed on another device, pulled 2026-04-14). New `api/lib/resend-mail.js` helper that routes through a Resend-hosted template (looked up via `RESEND_TEMPLATE_<NAME>_ID` env keys) when configured, falling back to inline HTML otherwise. Wired into contact/waitlist/waitlist-confirm/notify-signup + alerts path. Lets operators edit transactional email copy in the Resend dashboard without code deploys. — `api/lib/resend-mail.js`, `api/lib/alerts.js`, `api/contact.js`, `api/waitlist.js`, `api/waitlist-confirm.js`, `api/notify-signup.js`, `.env.example`
- 2026-04-13 — Landing page performance refactor (12 commits, `8072e17c`..`db0d0e77`, committed on another device, pulled 2026-04-14). `script.js` dropped from ~2,100 lines to ~700: 3D neuron background disabled by default and the Three.js setup extracted into the new 650-line `landing-background.js` (opt-in module). Landing page now renders statically with native CSS scroll (no rAF-driven smooth-scroll), removing the interaction overhead that made the page janky on lower-end devices. Atom electron animation slowed and motion tightened for readability. Typography brightened on both landing (`styles.css`) and the logged-in app shell (`shared/site.css`). — `script.js`, `landing-background.js`, `index.html`, `styles.css`, `shared/site.css`
- 2026-04-13 — Chat startup + nutrition progress pane fixes (commit `5b4023d3`, committed on another device, pulled 2026-04-14). Patched pipeline modules `confidence.js` (new file surfacing confidence deltas), `context.js`, `onboarding.js`, `stream.js` + `workflow.js` orchestrator seams, plus client-side progress pane wiring (`app/progress/nutrition-pane.js`, `progress.js`) and chat UI seams (`shared/react-chat-app.js`, `shared/chat.css`). Fixes post-pipeline-redesign regressions where the first chat turn could stall and the nutrition progress tab rendered stale state. — `api/emersus/pipeline/{confidence,context,onboarding,stream}.js`, `api/emersus/workflow.js`, `shared/react-chat-app.js`, `shared/chat.css`, `app/progress/nutrition-pane.js`, `app/progress/progress.js`
- 2026-04-13 — Nutrition UI text + macro ring polish (commits `91816eb5`, `db2a7809`, committed on another device, pulled 2026-04-14). Macro rings now animate on update, sizing + typography aligned with the new design system across food-detail drawer, journal, log-confirm widget, plan, supplements, and today panels. — `shared/nutrition-charts.js`, `shared/food-detail-drawer.js`, `shared/nutrition-journal-panel.js`, `shared/nutrition-log-confirm-widget.js`, `shared/nutrition-plan-panel.js`, `shared/nutrition-supplements-panel.js`, `shared/nutrition-today-panel.js`
- 2026-04-13 — Internal email-template mockups + screenshot-profile seed script (commit `249ef31b`, committed on another device, pulled 2026-04-14). New `internal/email-mockups/` directory (README documents why it lives outside any deployed static route) with `template-a.html` (confirm-identity) and `template-b.html` (confirmed) — source-only reference for the Resend-hosted templates. Added `scripts/seed-screenshot-profile.js` for populating demo profiles used in marketing screenshots. `.gitignore` tweaked to keep the rest of `internal/` local. — `internal/email-mockups/*`, `scripts/seed-screenshot-profile.js`, `.gitignore`
- 2026-04-15 — Frontend redesign **Phase 1 — Foundation** shipped + tagged `redesign-phase-1-foundation` (12 commits, `7a1462ab`..`326ac87f`). Delivered: (1) `shared/design-tokens.css` — Graphite·Jade (dark) + Paper·Royal (light) palettes as CSS custom properties scoped on `[data-theme="mint|paper"]`, base reset, Space Grotesk + JetBrains Mono font loading; (2) `shared/theme.js` — pure palette resolver (`validateTheme`, `resolveInitialTheme`, `VALID_THEMES`) + DOM appliers (`applyTheme`, `bootTheme`, `readSavedTheme`, `systemPrefersLight`, `bindSwitcher`) with `localStorage.emersus-theme` persistence + `prefers-color-scheme` fallback; (3) `shared/chrome.css` — palette switcher widget + app-shell grid + sidebar chrome (brand, nav items, thread list, user card, search, primary CTA) + top bar + centered + sub + period tabs + button variants (sm/icon/accent/danger) + autosave pulse + form controls (field, field-input, num-input, pill-group, toggle, card); (4) all 23 HTML pages (landing, chat, 11 `/app/**`, 5 `/auth/**`, `/contact`, `/privacy`, `/terms`, `/consumer-health-data`, `/demo`) load `design-tokens.css` + `theme.js`; authenticated pages + chat also load `chrome.css`. Visual changes are invisible to users at this point — no page has adopted the new chrome yet (that happens in phase 2+). Tests: 7 unit tests for theme.js pure logic + 3 file-presence tests. Also committed beforehand: legal(compliance) refresh adding `/consumer-health-data/` + expanded privacy/terms (commit `c14db3dc`). — `shared/design-tokens.css`, `shared/theme.js`, `shared/chrome.css`, `tests/unit/shared/theme.test.js`, `tests/unit/server.routes.test.js`, spec: `docs/superpowers/specs/2026-04-15-frontend-redesign-design.md`, plan: `docs/superpowers/plans/2026-04-15-redesign-phase-1-foundation.md`
- 2026-04-14 — Non-pubmed chunking + embedding backfill, end-to-end. Closed the phase-3 multi-source gap where 719k non-pubmed `research_articles` rows had zero `evidence_chunks` and were silently unretrievable. Shipped: (1) source-agnostic `scripts/lib/build-evidence-chunks-generic.js` — pure helper, title + abstract chunks, gated on abstract >= 50 chars; (2) `jobs/chunk-articles-gc.js` pg-boss handler + nightly cron (`30 3 * * *` ET, limit 5000/tick) that doubles as the backfill mechanism; (3) `scripts/backfill-chunks.js` CLI wrapper (pg.Pool + tagged-template adapter, `--source` / `--limit` / `--loop` / `--dry-run`); (4) inline chunk-insert in `jobs/ingest-topic-from-source.js` so forward-going ingests chunk automatically, failing soft so flaky chunking never breaks ingest. Backfill executed: sportrxiv canary (325 rows → 677 chunks, end-to-end retrieval probe returned synthetic-pmid hits), then full run processed 745k eligible rows producing **1,582,285 chunks** (~43 min wall clock, `unnest`-batched insert at ~530 rows/sec). Embedding drain: 1.34M chunks embedded in ~4 hrs via `embed-batch` at effective ~80 chunks/sec (≈ tier-2 OpenAI TPM ceiling, text-embedding-3-small). Perf work along the way: embed-batch UPDATE rewritten per-row → batched-unnest (40× faster DB writes), `max_memory_restart` raised 512 MB → 2 GB on pm2 (stops the SIGINT/restart loop that was losing in-flight batches), briefly set then reverted `embed-batch` concurrency=2 after discovering the handler lacks `FOR UPDATE SKIP LOCKED` and two handlers duplicate-embedded ~250k chunks for ~$1 of wasted OpenAI (~$7.30 total spend). HNSW index dropped before embed drain + rebuilt via `CREATE INDEX CONCURRENTLY` after (serial, `maintenance_work_mem=1GB`; ~3–4 hrs on 2.77M × 1536-dim vectors with default pgvector settings). Retrieval during the rebuild window uses seq scan. Final state: all non-pubmed sources retrievable via `match_evidence_chunks`; `evidence_chunks` grew 1.19M → 2.77M rows. 19 tests added across unit + integration. — `scripts/lib/build-evidence-chunks-generic.js`, `jobs/chunk-articles-gc.js`, `jobs/embed-batch.js`, `jobs/ingest-topic-from-source.js`, `jobs/_registry.js`, `scripts/backfill-chunks.js`, `tests/unit/lib/build-evidence-chunks-generic.test.js`, `tests/integration/jobs/chunk-articles-gc.test.js`, tests/unit tweaks, spec: `docs/superpowers/specs/2026-04-14-nonpubmed-chunking-embedding-design.md`, plan: `docs/superpowers/plans/2026-04-14-nonpubmed-chunking-embedding.md`
- 2026-04-25 — **Rerank-shootout 200-fixture eval + retrieval pipeline cleanup + hybrid v5 deployed (flag off).** Two commits (`7a64a980` retrieval pipeline fixes + `2396969a` eval harness). **Pipeline cleanup (live)**: removed `rankEvidence` from `pipeline/retrieve.js` (was overwriting Jina/zerank scores with the heuristic freshness/quality blend); fixed dedup ordering in `retrieveDatabaseEvidence.js` so Jina runs before `dedupByDoi` instead of after; removed the `useV4 ? enriched : dedupByDoi(enriched)` short-circuit so V4 path always dedupes; updated `dedupByDoi` tiebreaker to `_jina_score > rrf_score > similarity`; bumped `VECTOR_MATCH_COUNT` 25 → 40 and `VECTOR_LIMIT` 6 → 8; flipped `groundingEnforcementEnabled()` to opt-out (was opt-in); removed `computeConfidence` calls from `workflow.js` (pending rework). **Hybrid v5 (deployed but flag OFF)**: applied `match_evidence_chunks_hybrid_v5` migration via direct psql (CREATE INDEX CONCURRENTLY GIN on `to_tsvector('english', content)`, ~25 min on 3.34M rows); patched `ts_rank_cd` real → double precision cast bug discovered post-deploy; tuned with asymmetric RRF (`p_dense_weight` defaults 0.7, `lex_weight` 0.3) after first eval showed BM25 noise was overwhelming cosine signal; dropped 7-param overload after introducing 8-param. **Eval harness expansion**: added DOI-level recall metrics to `bench-matrix.js` (collapses cross-source pmid duplicates — pmid-A pubmed and pmid-B semantic-scholar with same DOI count as one canonical paper, resolved a false "regression" in the v5 results); added `zerank` (ZeroEntropy zerank-2) and `selfhost` (Text Embeddings Inference REST) backends to `cross-rerank.js`; added Cohere 429 retry with 7s backoff for trial keys; new `paired-stats.js` (bootstrap CIs + paired Wilcoxon signed-rank with Bonferroni correction + listwise deletion of incomplete pairs); new `merge-log-and-json.js` (recovers partial bench runs by parsing log + merging with JSON outputs); new `quick-aggregate.js` (live progress aggregator). Self-hosted BGE-reranker-base on Hetzner via `bge-rerank` docker container (TEI cpu-latest, ~600MB RAM, port 8081 loopback). **Findings (200 paired fixtures)**: only Z2 (HyDE + dense + zerank-2) is significantly better than baseline at Bonferroni α=0.0056 (+10pp doi@10, p=0.005). Z2 vs Z2H (adds BM25): -1.5pp NOT sig — **HyDE and BM25 are redundant** (both close vocabulary gap from opposite sides; doing both creates noisier candidate pool that hurts the reranker). Z2 vs C2 (HyDE+Cohere): +3.2pp NOT sig — zerank-2 statistically equivalent to Cohere v3.5 on this corpus, so pick on price (zerank is half: $0.025/M tok vs $0.05/M). BGE-reranker-base actively hurts (-5.5pp directional, marginal p=0.064) and is slow on CPU (6-13s/call) — wrong model for biomedical text. **Two configs identified, ship pending**: Config A (Z2: HyDE+dense+zerank, +10pp ✓ stat-sig, ~$0.001/query, +3.6s) vs Config B (Z1H: hybrid+zerank no-HyDE, +4.5pp directional, ~$0.0006/query, +1.2s). **Currently in prod**: S3 (HyDE on, no rerank) — paying HyDE's 2s latency for 0pp lift over baseline, worst of both worlds. SessionStart hook installed in `.claude/settings.json` to surface the pending decision in every new session. **Caveat on the result**: contextual embeddings infrastructure already in repo but not backfilled; per Anthropic's contextual retrieval paper, contextual + dense alone could close ~half the Z2 gap, which would change the cost-benefit. Should re-run this eval after contextual embedding backfill completes. — `api/emersus/retrieveDatabaseEvidence.js`, `api/emersus/pipeline/retrieve.js`, `api/emersus/pipeline/prompt.js`, `api/emersus/workflow.js`, `supabase/20260424_match_evidence_chunks_hybrid_v5.sql`, `scripts/eval/bench-matrix.js`, `scripts/eval/lib/cross-rerank.js`, `scripts/eval/stacks.json`, `scripts/eval/paired-stats.js`, `scripts/eval/merge-log-and-json.js`, `scripts/eval/quick-aggregate.js`, `.claude/settings.json`, `.claude/retrieval-reminder.json`, memory: `project_retrieval_eval_2026-04-25.md`, `project_rerank_shootout_2026-04-25_prelim.md`, `feedback_webhook_stashes_eval_edits.md`
